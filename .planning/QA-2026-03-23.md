# QA Session — 2026-03-23

Manual testing of release APK after Phase 08 completion.

## P0 — Crashes
1. **Long press diary item → hard crash** — reproduce on any food diary entry
7. **Gallery scan → `cannot read property length of undefined`** — tap "Scan Gallery" button
10. **Health Connect toggle → crash** — toggle in settings

## P1 — Broken Features
5. **OFF alternative salmon not updating nutrition** — selecting OFF DB alternative dish doesn't change displayed macros
8. **Google Sign-in DEVELOPER_ERROR** — after choosing account, modal shows DEVELOPER_ERROR (likely SHA-1 fingerprint mismatch in Google Cloud Console)
11. **Region setting is noop** — tapping region in settings does nothing
12. **Metric units setting is noop** — tapping metric units in settings does nothing

## P1 — Missing Display
4. **No per-ingredient macro breakdown after scan** — only total calories shown, not per-ingredient macros

## P2 — UX Issues
2. **Re-log meal from home page** — tap triggers action directly instead of showing contextual menu (re-log, edit, view, delete). Needs UX research on whether home is even the right surface.
3. **Third toggle view in diary has no nutrition** — three-way toggle on diary items, third view is empty. Remove it, keep two: macros + ingredient breakdown.
6. **No barcode option on add food screen** — only picture, food DB, gallery available
9. **Container weights — no identifiers** — settings shows container weight management but no way to label/identify containers
