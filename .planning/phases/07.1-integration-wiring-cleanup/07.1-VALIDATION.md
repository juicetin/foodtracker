---
phase: 7.1
slug: integration-wiring-cleanup
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-21
---

# Phase 7.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest via jest-expo |
| **Config file** | `apps/mobile/jest.config.js` |
| **Quick run command** | `cd apps/mobile && npx jest --testPathPattern='<pattern>' --no-coverage -x` |
| **Full suite command** | `cd apps/mobile && npx jest --no-coverage` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd apps/mobile && npx jest --testPathPattern='<changed-test>' --no-coverage -x`
- **After every plan wave:** Run `cd apps/mobile && npx jest --no-coverage`
- **Before `/gsd:verify-work`:** Full suite must be green + `npx tsc --noEmit`
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 07.1-01-01 | 01 | 1 | GAP-01 | unit | `npx jest --testPathPattern='foodClassifier.test' -x` | ✅ (needs update) | ⬜ pending |
| 07.1-01-02 | 01 | 1 | GAP-02 | unit | `npx jest --testPathPattern='ScaleInput' -x` | ❌ W0 | ⬜ pending |
| 07.1-01-03 | 01 | 1 | GAP-03 | build | `npx tsc --noEmit` | n/a (build check) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `services/gallery/__tests__/foodClassifier.test.ts` — update to verify diary entry creation from drainScanQueue
- [ ] Create `__tests__/ScaleInputScreen.test.tsx` — stub for netWeight callback return
- [ ] Delete `services/detection/__tests__/portionBridge.test.ts` — tests deleted code
- [ ] Delete `services/vlm/__tests__/vlmService.test.ts` — tests deleted code

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Gallery scan → diary entries visible in UI | GAP-01 | Requires full React Navigation + store integration | Open gallery scanner, scan photo, verify diary tab shows entries |
| Scale input → weight returned to caller | GAP-02 | Requires navigation params callback chain | Navigate to ScaleInputScreen, enter weight, confirm, verify caller receives value |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
