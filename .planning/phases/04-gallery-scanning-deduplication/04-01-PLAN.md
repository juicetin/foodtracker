---
phase: 04-gallery-scanning-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/db/schema.ts
  - apps/mobile/db/migrations/0010_gallery_scan.sql
  - apps/mobile/src/services/gallery/galleryScanService.ts
  - apps/mobile/src/services/gallery/foodClassifier.ts
  - apps/mobile/src/services/gallery/mealGrouper.ts
  - apps/mobile/src/services/gallery/photoImporter.ts
  - apps/mobile/src/services/gallery/types.ts
  - apps/mobile/src/services/gallery/__tests__/galleryScanService.test.ts
  - apps/mobile/src/services/gallery/__tests__/mealGrouper.test.ts
  - apps/mobile/src/services/gallery/__tests__/photoImporter.test.ts
  - apps/mobile/src/services/gallery/__tests__/foodClassifier.test.ts
  - apps/mobile/src/services/gallery/index.ts
autonomous: true
requirements:
  - GAL-01
  - GAL-03
  - GAL-04
  - GAL-05

must_haves:
  truths:
    - "Gallery photos are enumerated incrementally using cursor-based pagination (no full gallery load)"
    - "Food photos are classified via Gemini Nano (foreground only) and non-food photos are marked as done"
    - "Multiple photos within 1-hour window + 150m GPS proximity are grouped into a single meal event"
    - "Confirmed food photos are downscaled to 1024px longest edge and copied to app storage"
    - "EXIF metadata (timestamp, GPS) is extracted and stored with each queued photo"
  artifacts:
    - path: "apps/mobile/src/services/gallery/types.ts"
      provides: "Type contracts for gallery scanning"
      exports: ["ScanQueueItem", "ClassifiedPhoto", "MealGroup", "GalleryScanPreferences"]
    - path: "apps/mobile/src/services/gallery/galleryScanService.ts"
      provides: "Core scan logic: enumerate, filter, queue"
      exports: ["discoverNewPhotos", "getLastScanTimestamp", "setLastScanTimestamp", "getPendingScanItems", "markScanItemDone", "insertScanQueueItem"]
    - path: "apps/mobile/src/services/gallery/mealGrouper.ts"
      provides: "Temporal + GPS clustering"
      exports: ["groupIntoMeals", "haversineDistance"]
    - path: "apps/mobile/src/services/gallery/photoImporter.ts"
      provides: "Downscale + copy to app storage"
      exports: ["importPhoto"]
    - path: "apps/mobile/src/services/gallery/foodClassifier.ts"
      provides: "Gemini Nano food/not-food classification"
      exports: ["classifyPhoto", "drainScanQueue"]
  key_links:
    - from: "apps/mobile/src/services/gallery/galleryScanService.ts"
      to: "expo-media-library"
      via: "getAssetsAsync with pagination"
      pattern: "MediaLibrary\\.getAssetsAsync"
    - from: "apps/mobile/src/services/gallery/foodClassifier.ts"
      to: "apps/mobile/src/services/vlm/geminiNanoService.ts"
      via: "geminiNanoService.identify()"
      pattern: "geminiNanoService\\.identify"
    - from: "apps/mobile/src/services/gallery/mealGrouper.ts"
      to: "apps/mobile/src/services/gallery/types.ts"
      via: "ClassifiedPhoto -> MealGroup clustering"
      pattern: "groupIntoMeals"
---

<objective>
Gallery scan service layer: schema extension, type contracts, core services (discovery, classification, meal grouping, photo import), and unit tests.

Purpose: Build the foundational service layer for gallery scanning. All four core services -- photo discovery, food classification, meal grouping, and photo import -- are created with full test coverage. Plan 02 wires these into background scheduling and UI.

Output: 4 service modules, type contracts, schema migration, unit tests
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gallery-scanning-deduplication/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/mobile/db/schema.ts (existing scan_queue -- needs extension):
```typescript
export const scanQueue = sqliteTable('scan_queue', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  assetId: text('asset_id'),
  uri: text('uri').notNull(),
  status: text('status').notNull().default('pending'),
  createdAt: text('created_at').default(sql`(datetime('now'))`),
  processedAt: text('processed_at'),
});
```

From apps/mobile/src/services/vlm/geminiNanoService.ts:
```typescript
export const geminiNanoService = {
  async isAvailable(): Promise<boolean>;
  async identify(photoUri: string, _userText?: string): Promise<VlmFoodResult>;
};
// VlmFoodResult = { dishes: { name, cuisine, recipe_name, ingredients: {name, amount_g}[] }[] }
// Empty dishes array = "not food"
```

From apps/mobile/src/types/index.ts:
```typescript
export interface Photo {
  id: string;
  entryId: string;
  uri: string;
  localPath: string | null;
  width: number | null;
  height: number | null;
}
```

From apps/mobile/src/services/backup/backupScheduler.ts (pattern to follow):
```typescript
import * as TaskManager from 'expo-task-manager';
import * as BackgroundTask from 'expo-background-task';
// defineTask at module scope, side-effect import in App.tsx
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema extension + type contracts + mealGrouper + photoImporter with tests</name>
  <files>
    apps/mobile/db/schema.ts,
    apps/mobile/db/migrations/0010_gallery_scan.sql,
    apps/mobile/src/services/gallery/types.ts,
    apps/mobile/src/services/gallery/mealGrouper.ts,
    apps/mobile/src/services/gallery/photoImporter.ts,
    apps/mobile/src/services/gallery/__tests__/mealGrouper.test.ts,
    apps/mobile/src/services/gallery/__tests__/photoImporter.test.ts
  </files>
  <read_first>
    apps/mobile/db/schema.ts,
    apps/mobile/db/migrations,
    apps/mobile/src/types/index.ts,
    apps/mobile/src/services/backup/backupScheduler.ts
  </read_first>
  <behavior>
    - mealGrouper: 2 photos 30min apart, same GPS -> grouped into 1 meal
    - mealGrouper: 2 photos 2hr apart -> separate meals
    - mealGrouper: 2 photos 30min apart, one missing GPS -> grouped by time only
    - mealGrouper: 2 photos 30min apart, GPS 500m apart -> separate meals
    - mealGrouper: 5 photos spanning 45min -> all grouped into 1 meal
    - haversineDistance: known coords return expected distance within 1% tolerance
    - photoImporter: downscales >1024px photo and returns persistent path (mock manipulateAsync + File)
    - photoImporter: does not resize photo already <=1024px
  </behavior>
  <action>
    1. Extend `scanQueue` in db/schema.ts: add columns `creationTime` (integer, photo EXIF epoch ms), `latitude` (real, nullable), `longitude` (real, nullable), `isFood` (integer/boolean, nullable), `mealGroupId` (text, nullable). Add UNIQUE constraint on `assetId` column.

    2. Create migration `0010_gallery_scan.sql` with ALTER TABLE statements adding the new columns. Check existing migration numbering pattern first.

    3. Create `src/services/gallery/types.ts` with:
       - `ScanQueueItem`: { id, assetId, uri, status, creationTime, latitude?, longitude?, isFood?, mealGroupId?, createdAt, processedAt? }
       - `ClassifiedPhoto`: { id, assetId, uri, creationTime, latitude?, longitude?, isFood: boolean }
       - `MealGroup`: { id: string, photos: ClassifiedPhoto[], firstTimestamp: number, lastTimestamp: number, location?: { latitude: number, longitude: number } }
       - `GalleryScanPreferences`: { mealWindowMs: number, gpsProximityM: number, scanChunkSize: number, firstScanDaysBack: number }
       - `DEFAULT_SCAN_PREFS`: { mealWindowMs: 3600000, gpsProximityM: 150, scanChunkSize: 50, firstScanDaysBack: 30 }

    4. Create `src/services/gallery/mealGrouper.ts`:
       - `haversineDistance(lat1, lon1, lat2, lon2)` -> meters (Earth radius 6371000m)
       - `groupIntoMeals(photos: ClassifiedPhoto[], prefs?: Partial<GalleryScanPreferences>)` -> MealGroup[]
       - Sort photos by creationTime ascending
       - For each photo, find existing group where: (a) time diff <= mealWindowMs AND (b) if both have GPS, distance <= gpsProximityM; if either lacks GPS, group by time only
       - Generate meal group IDs with `crypto.randomUUID()`

    5. Create `src/services/gallery/photoImporter.ts`:
       - `importPhoto(galleryUri: string, assetId: string)` -> Promise<string> (returns persistent file path)
       - Use `manipulateAsync` (legacy API per project convention) to downscale to 1024px longest edge, JPEG 0.8 compress
       - Move result from cache to `${Paths.document}/gallery-imports/${generateId()}.jpg`
       - Create `gallery-imports` directory if not exists
       - Do NOT resize if longest edge <= 1024

    6. Write tests FIRST (TDD):
       - `mealGrouper.test.ts`: all behavior cases listed above
       - `photoImporter.test.ts`: mock expo-image-manipulator and expo-file-system/next, verify resize logic and file move
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest src/services/gallery/__tests__/mealGrouper.test.ts src/services/gallery/__tests__/photoImporter.test.ts --no-coverage -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "creationTime" apps/mobile/db/schema.ts
    - grep -q "mealGroupId" apps/mobile/db/schema.ts
    - grep -q "UNIQUE" apps/mobile/db/schema.ts || grep -q "unique" apps/mobile/db/schema.ts
    - grep -q "ScanQueueItem" apps/mobile/src/services/gallery/types.ts
    - grep -q "MealGroup" apps/mobile/src/services/gallery/types.ts
    - grep -q "GalleryScanPreferences" apps/mobile/src/services/gallery/types.ts
    - grep -q "haversineDistance" apps/mobile/src/services/gallery/mealGrouper.ts
    - grep -q "groupIntoMeals" apps/mobile/src/services/gallery/mealGrouper.ts
    - grep -q "importPhoto" apps/mobile/src/services/gallery/photoImporter.ts
    - grep -q "1024" apps/mobile/src/services/gallery/photoImporter.ts
    - test -f apps/mobile/src/services/gallery/__tests__/mealGrouper.test.ts
    - test -f apps/mobile/src/services/gallery/__tests__/photoImporter.test.ts
  </acceptance_criteria>
  <done>
    Schema extended with gallery scan columns. Type contracts exported. mealGrouper correctly clusters photos by 1hr+150m proximity with time-only fallback when GPS missing. photoImporter downscales to 1024px and persists to app storage. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: galleryScanService + foodClassifier + barrel index with tests</name>
  <files>
    apps/mobile/src/services/gallery/galleryScanService.ts,
    apps/mobile/src/services/gallery/foodClassifier.ts,
    apps/mobile/src/services/gallery/index.ts,
    apps/mobile/src/services/gallery/__tests__/galleryScanService.test.ts,
    apps/mobile/src/services/gallery/__tests__/foodClassifier.test.ts
  </files>
  <read_first>
    apps/mobile/src/services/gallery/types.ts,
    apps/mobile/src/services/gallery/mealGrouper.ts,
    apps/mobile/src/services/gallery/photoImporter.ts,
    apps/mobile/src/services/vlm/geminiNanoService.ts,
    apps/mobile/db/schema.ts
  </read_first>
  <behavior>
    - discoverNewPhotos: calls getAssetsAsync with createdAfter filter and chunk size, inserts new assets into scan_queue with INSERT OR IGNORE
    - discoverNewPhotos: extracts EXIF (creationTime, lat, lon) via getAssetInfoAsync for each new asset
    - discoverNewPhotos: updates lastScanTimestamp after processing chunk
    - classifyPhoto: calls geminiNanoService.identify, returns true if dishes.length > 0
    - drainScanQueue: processes pending items, classifies each, marks isFood, groups food photos into meals, imports food photos
    - drainScanQueue: respects 500ms delay between classifications (Gemini Nano throttling)
    - drainScanQueue: stops on BUSY/error and retries with backoff
  </behavior>
  <action>
    1. Create `src/services/gallery/galleryScanService.ts`:
       - `discoverNewPhotos(chunkSize?: number)` -> Promise<number> (count of new photos queued)
         - Get lastScanTimestamp from SQLite (or default 30 days back)
         - Call `MediaLibrary.getAssetsAsync({ first: chunkSize ?? 50, mediaType: 'photo', sortBy: ['creationTime', true], createdAfter: lastTs })`
         - For each asset, call `MediaLibrary.getAssetInfoAsync(asset.id)` to get EXIF location
         - INSERT OR IGNORE into scan_queue with assetId (unique key), uri, creationTime, latitude, longitude, status='pending'
         - Update lastScanTimestamp to max creationTime in batch
         - Wrap getAssetInfoAsync in try/catch (some photos lack EXIF)
       - `getLastScanTimestamp()` -> Promise<number> (from SQLite key-value or user_settings)
       - `setLastScanTimestamp(ts: number)` -> Promise<void>
       - `getPendingScanItems(limit?: number)` -> Promise<ScanQueueItem[]>
       - `markScanItemDone(id: number, isFood: boolean, mealGroupId?: string)` -> Promise<void>
       - `insertScanQueueItem(...)` (internal, used by discoverNewPhotos)
       - Use raw SQL via `userDb.execute()` pattern (consistent with project convention)

    2. Create `src/services/gallery/foodClassifier.ts`:
       - `classifyPhoto(photoUri: string)` -> Promise<boolean>
         - Calls `geminiNanoService.identify(photoUri)` and returns `result.dishes.length > 0`
       - `drainScanQueue(options?: { batchSize?: number, onProgress?: (done: number, total: number) => void })` -> Promise<{ classified: number, foodPhotos: number, mealGroups: number }>
         - Get pending items from scan_queue
         - For each: classify via Gemini Nano, mark isFood in DB
         - Filter food photos, call `groupIntoMeals(foodPhotos)`
         - For each meal group: call `importPhoto()` for each photo, update mealGroupId in DB
         - 500ms delay between classify calls (avoid AICore BUSY)
         - On error/BUSY: exponential backoff (1s, 2s, 4s), max 3 retries per item, then mark as 'error'
         - Call onProgress callback after each item

    3. Create `src/services/gallery/index.ts` barrel export:
       - Re-export all public functions from galleryScanService, foodClassifier, mealGrouper, photoImporter, types

    4. Write tests FIRST (TDD):
       - `galleryScanService.test.ts`: mock expo-media-library (getAssetsAsync, getAssetInfoAsync), mock op-sqlite userDb. Test pagination, EXIF extraction, INSERT OR IGNORE dedup, timestamp tracking.
       - `foodClassifier.test.ts`: mock geminiNanoService. Test food detection, not-food detection, drainScanQueue batch processing, progress callback, error handling with backoff.
       - Create `__mocks__/expo-media-library.ts` if not already present (mock getAssetsAsync, getAssetInfoAsync, requestPermissionsAsync).
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest src/services/gallery/__tests__/ --no-coverage -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "discoverNewPhotos" apps/mobile/src/services/gallery/galleryScanService.ts
    - grep -q "getAssetsAsync" apps/mobile/src/services/gallery/galleryScanService.ts
    - grep -q "getAssetInfoAsync" apps/mobile/src/services/gallery/galleryScanService.ts
    - grep -q "INSERT OR IGNORE" apps/mobile/src/services/gallery/galleryScanService.ts
    - grep -q "classifyPhoto" apps/mobile/src/services/gallery/foodClassifier.ts
    - grep -q "drainScanQueue" apps/mobile/src/services/gallery/foodClassifier.ts
    - grep -q "geminiNanoService" apps/mobile/src/services/gallery/foodClassifier.ts
    - grep -q "500" apps/mobile/src/services/gallery/foodClassifier.ts
    - test -f apps/mobile/src/services/gallery/index.ts
    - test -f apps/mobile/src/services/gallery/__tests__/galleryScanService.test.ts
    - test -f apps/mobile/src/services/gallery/__tests__/foodClassifier.test.ts
  </acceptance_criteria>
  <done>
    galleryScanService enumerates gallery with cursor pagination, extracts EXIF, deduplicates via assetId UNIQUE. foodClassifier wraps Gemini Nano for food/not-food with 500ms pacing and exponential backoff. drainScanQueue orchestrates classification -> grouping -> import pipeline. Barrel index exports all public APIs. All tests pass.
  </done>
</task>

</tasks>

<verification>
cd apps/mobile && npx jest src/services/gallery/__tests__/ --no-coverage -x
</verification>

<success_criteria>
- All 4 gallery service modules exist with exported functions
- Schema extended with EXIF columns and UNIQUE constraint on assetId
- Type contracts cover all gallery scan data structures
- mealGrouper correctly handles time-only and time+GPS clustering
- photoImporter downscales to 1024px and persists to app document storage
- galleryScanService uses cursor pagination with INSERT OR IGNORE
- foodClassifier wraps Gemini Nano with 500ms pacing
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-gallery-scanning-deduplication/04-01-SUMMARY.md`
</output>
