---
phase: 09-ux-redesign
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/mobile/src/screens/AddFoodScreen.tsx
  - apps/mobile/src/components/add-food/AddFoodSearchBar.tsx
  - apps/mobile/src/components/add-food/QuickAccessTabs.tsx
  - apps/mobile/src/components/add-food/EntryMethodCards.tsx
  - apps/mobile/src/screens/index.ts
  - apps/mobile/src/navigation/RootNavigator.tsx
autonomous: true
requirements:
  - UX-06
  - UX-07
  - UX-08
  - UX-09
  - QA-06

must_haves:
  truths:
    - "AddFoodScreen has search bar with camera, voice, and barcode icons"
    - "Quick access tabs show Recent, Frequent, Favorites, My Recipes"
    - "Entry method cards show Scan Photo, Scan Barcode, Quick Add Macros, From Gallery"
    - "Tapping camera icon navigates to DetectionScreen"
    - "Tapping barcode icon navigates to BarcodeScanScreen"
    - "Barcode option is always visible on the add food screen (QA-06 fix)"
  artifacts:
    - path: "apps/mobile/src/screens/AddFoodScreen.tsx"
      provides: "Unified add food hub screen"
      contains: "export default function AddFoodScreen"
    - path: "apps/mobile/src/components/add-food/AddFoodSearchBar.tsx"
      provides: "Search input with camera/voice/barcode icons"
      exports: ["AddFoodSearchBar"]
    - path: "apps/mobile/src/components/add-food/QuickAccessTabs.tsx"
      provides: "Recent/Frequent/Favorites/Recipes tabs"
      exports: ["QuickAccessTabs"]
    - path: "apps/mobile/src/components/add-food/EntryMethodCards.tsx"
      provides: "Scan Photo/Barcode/Quick Add/Gallery card grid"
      exports: ["EntryMethodCards"]
  key_links:
    - from: "AddFoodScreen.tsx"
      to: "DetectionScreen"
      via: "navigation.navigate('Detection')"
      pattern: "navigate.*Detection"
    - from: "AddFoodScreen.tsx"
      to: "BarcodeScanScreen"
      via: "navigation.navigate('BarcodeScan')"
      pattern: "navigate.*BarcodeScan"
    - from: "QuickAccessTabs.tsx"
      to: "historyService.ts"
      via: "getRecentHistory import"
      pattern: "getRecentHistory"
---

<objective>
Build the unified AddFoodScreen with search bar, quick access tabs (Recent/Frequent/Favorites/Recipes), and entry method cards (Scan Photo/Barcode/Quick Add/Gallery). This is the new hub for all food entry methods.

Purpose: Consolidates all food entry paths into one screen. Fixes QA-06 (barcode always visible). Reduces cognitive load by showing all methods in one place.
Output: AddFoodScreen and supporting components, wired into RootNavigator.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ux-redesign-diary-first-home-add-food-with-barcode-photo-voice-gallery-item-detail-bottom-sheet-long-press-context-menu-copy-move-meals/09-CONTEXT.md
@.planning/phases/09-ux-redesign-diary-first-home-add-food-with-barcode-photo-voice-gallery-item-detail-bottom-sheet-long-press-context-menu-copy-move-meals/09-UI-SPEC.md
@.planning/phases/09-ux-redesign-diary-first-home-add-food-with-barcode-photo-voice-gallery-item-detail-bottom-sheet-long-press-context-menu-copy-move-meals/09-01-SUMMARY.md

<interfaces>
<!-- From historyService.ts -->
export function getRecentHistory(limit?: number): HistoryItem[];
export function searchHistory(query: string, limit?: number): HistoryItem[];

<!-- From favourites.ts -->
export function loadFavourites(limit?: number): FavouriteMeal[];

<!-- From services/search (existing FoodSearchScreen pattern) -->
<!-- FoodSearchScreen queries KG + OFF for results -->

<!-- From types/index.ts (updated in Plan 01) -->
export type RootStackParamList = {
  AddFood: { mealType?: string };
  Detection: undefined;
  BarcodeScan: undefined;
  FoodSearch: undefined;
  QuickAdd: undefined;
  ...
};

<!-- From mealGroups.ts (Plan 01) -->
export const MEAL_GROUP_CONFIG: Record<MealGroup, { label: string; icon: string }>;

<!-- From services/detection/types.ts -->
export function autoDetectMealType(): string;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AddFood components</name>
  <files>
    apps/mobile/src/components/add-food/AddFoodSearchBar.tsx,
    apps/mobile/src/components/add-food/QuickAccessTabs.tsx,
    apps/mobile/src/components/add-food/EntryMethodCards.tsx
  </files>
  <read_first>
    apps/mobile/src/screens/FoodSearchScreen.tsx,
    apps/mobile/src/services/search/historyService.ts,
    apps/mobile/src/services/favourites.ts,
    apps/mobile/src/services/detection/types.ts,
    apps/mobile/src/types/index.ts
  </read_first>
  <action>
1. Create directory `src/components/add-food/` if not exists.

2. **AddFoodSearchBar.tsx**:
   - Props: `{ value: string; onChangeText: (text: string) => void; onCameraPress: () => void; onVoicePress: () => void; onBarcodePress: () => void; onSubmit: () => void }`
   - Render: TextInput with search icon (Ionicons search-outline) on left
   - Right side icons row: camera-outline, mic-outline, barcode-outline (all Ionicons)
   - Each icon is a Pressable with minWidth/minHeight 44px (touch target)
   - Icon colors: #6B7280 default, #16A34A when active/focused
   - Container: backgroundColor #FFFFFF, borderRadius 12px, paddingHorizontal 12px, paddingVertical 8px, borderWidth 1, borderColor #E5E7EB
   - TextInput: placeholder "Search food...", fontSize 16, flex 1, color #111827
   - On submit (return key): calls onSubmit
   - Voice icon press: calls onVoicePress (handler in AddFoodScreen will show a text input with note "Use keyboard voice input" since speech-to-text library deferred)

3. **QuickAccessTabs.tsx**:
   - Props: `{ activeTab: string; onTabChange: (tab: string) => void; items: QuickAccessItem[]; onItemPress: (item: QuickAccessItem) => void }`
   - Type `QuickAccessItem = { id: string; name: string; calories: number; protein: number; carbs: number; fat: number; source: 'history' | 'frequent' | 'favourite' | 'recipe' }`
   - Horizontal tab row: "Recent", "Frequent", "Favorites", "My Recipes"
   - Active tab: backgroundColor #16A34A, text #FFFFFF, borderRadius 20px (pill shape)
   - Inactive tab: backgroundColor #F3F4F6, text #374151
   - Tab font: 14px weight 600
   - Below tabs: vertical list of items (ScrollView)
   - Each item row: name (16px weight 600 #111827), cal value (14px #374151), Pressable -> onItemPress
   - Empty states per tab:
     - Recent: "No recent foods" / "Foods you log will appear here for quick re-logging."
     - Favorites: "No favorites yet" / "Long-press any food item and tap Save as Favorite."
     - Recipes: "No saved recipes" / "Save a meal as a recipe from the item detail sheet."
   - Tab height: 36px, paddingHorizontal 16px, gap 8px between tabs

4. **EntryMethodCards.tsx**:
   - Props: `{ onScanPhoto: () => void; onScanBarcode: () => void; onQuickAdd: () => void; onFromGallery: () => void }`
   - Render 2x2 grid of cards:
     - "Scan Photo": icon camera-outline, onPress calls onScanPhoto
     - "Scan Barcode": icon barcode-outline, onPress calls onScanBarcode
     - "Quick Add Macros": icon calculator-outline, onPress calls onQuickAdd
     - "From Gallery": icon images-outline, onPress calls onFromGallery
   - Card style: backgroundColor #FFFFFF, borderRadius 12px, padding 16px, elevation 1, shadowOpacity 0.05
   - Icon size 32, color #16A34A
   - Label below icon: 14px weight 600, color #374151, textAlign center
   - Grid: flexDirection row, flexWrap wrap, gap 12px, each card width ~47%
  </action>
  <verify>
    <automated>cd /home/me/repos/personal/foodtracker/apps/mobile && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/components/add-food/AddFoodSearchBar.tsx contains `export function AddFoodSearchBar`
    - apps/mobile/src/components/add-food/AddFoodSearchBar.tsx contains `camera-outline`
    - apps/mobile/src/components/add-food/AddFoodSearchBar.tsx contains `barcode-outline`
    - apps/mobile/src/components/add-food/AddFoodSearchBar.tsx contains `mic-outline`
    - apps/mobile/src/components/add-food/AddFoodSearchBar.tsx contains `Search food...`
    - apps/mobile/src/components/add-food/QuickAccessTabs.tsx contains `export function QuickAccessTabs`
    - apps/mobile/src/components/add-food/QuickAccessTabs.tsx contains `Recent`
    - apps/mobile/src/components/add-food/QuickAccessTabs.tsx contains `Frequent`
    - apps/mobile/src/components/add-food/QuickAccessTabs.tsx contains `Favorites`
    - apps/mobile/src/components/add-food/QuickAccessTabs.tsx contains `My Recipes`
    - apps/mobile/src/components/add-food/EntryMethodCards.tsx contains `export function EntryMethodCards`
    - apps/mobile/src/components/add-food/EntryMethodCards.tsx contains `Scan Photo`
    - apps/mobile/src/components/add-food/EntryMethodCards.tsx contains `Scan Barcode`
    - apps/mobile/src/components/add-food/EntryMethodCards.tsx contains `Quick Add Macros`
    - apps/mobile/src/components/add-food/EntryMethodCards.tsx contains `From Gallery`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Three add-food components created: search bar with camera/voice/barcode icons, quick access tabs with 4 tab types, entry method card grid with 4 methods.</done>
</task>

<task type="auto">
  <name>Task 2: Create AddFoodScreen and wire into navigator</name>
  <files>
    apps/mobile/src/screens/AddFoodScreen.tsx,
    apps/mobile/src/screens/index.ts,
    apps/mobile/src/navigation/RootNavigator.tsx
  </files>
  <read_first>
    apps/mobile/src/screens/FoodSearchScreen.tsx,
    apps/mobile/src/navigation/RootNavigator.tsx,
    apps/mobile/src/screens/index.ts,
    apps/mobile/src/services/search/historyService.ts,
    apps/mobile/src/services/favourites.ts,
    apps/mobile/src/types/index.ts,
    apps/mobile/src/components/add-food/AddFoodSearchBar.tsx,
    apps/mobile/src/components/add-food/QuickAccessTabs.tsx,
    apps/mobile/src/components/add-food/EntryMethodCards.tsx
  </read_first>
  <action>
1. **Create `src/screens/AddFoodScreen.tsx`**:
   - Receives route params: `{ mealType?: string }` from RootStackParamList
   - Import AddFoodSearchBar, QuickAccessTabs, EntryMethodCards from `../components/add-food/`
   - Import navigation hooks, getRecentHistory, loadFavourites, autoDetectMealType

   State:
   - `searchQuery: string`
   - `activeTab: 'recent' | 'frequent' | 'favorites' | 'recipes'` (default 'recent')
   - `tabItems: QuickAccessItem[]`
   - `mealType: string` (from route params or autoDetectMealType())

   Data loading (useEffect on activeTab):
   - 'recent': call getRecentHistory(20) -> map to QuickAccessItem format
   - 'frequent': call getRecentHistory(20) with frequency sort (reuse history service with frequency ranking)
   - 'favorites': call loadFavourites(20) -> map to QuickAccessItem format
   - 'recipes': query custom_recipes table for saved recipes -> map to QuickAccessItem format

   Search behavior:
   - When searchQuery length >= 2, navigate to FoodSearchScreen (existing) with the query
   - Or inline results if simpler — use existing search from FoodSearchScreen pattern

   Action handlers:
   - onCameraPress: `navigation.navigate('Detection')`
   - onBarcodePress: `navigation.navigate('BarcodeScan')`
   - onVoicePress: show Alert with message "Use your keyboard's voice input button to dictate food names" (speech-to-text library deferred)
   - onQuickAdd: `navigation.navigate('QuickAdd')`
   - onFromGallery: use `expo-image-picker` launchImageLibraryAsync, then navigate to Detection with the selected image URI
   - onItemPress: navigate to FoodSearch or directly to a food detail/confirm screen depending on item source

   Layout (top to bottom):
   - Header bar: "Add Food" title (20px weight 600), close/back button (X icon)
   - Meal type pill: shows current meal type (e.g., "Breakfast") with dropdown to change
   - AddFoodSearchBar
   - EntryMethodCards (2x2 grid)
   - QuickAccessTabs (with scrollable item list)

   Styling:
   - Screen background: #F5F5F5
   - Padding: 16px horizontal

2. **Update `src/screens/index.ts`**:
   - Add `export { default as AddFoodScreen } from './AddFoodScreen';`

3. **Update `src/navigation/RootNavigator.tsx`**:
   - Replace AddFoodPlaceholder with actual AddFoodScreen import
   - `import { AddFoodScreen } from '../screens';`
   - Stack.Screen name="AddFood" component={AddFoodScreen} with `animation: 'slide_from_right'`
  </action>
  <verify>
    <automated>cd /home/me/repos/personal/foodtracker/apps/mobile && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/screens/AddFoodScreen.tsx contains `export default function AddFoodScreen`
    - apps/mobile/src/screens/AddFoodScreen.tsx contains `AddFoodSearchBar`
    - apps/mobile/src/screens/AddFoodScreen.tsx contains `QuickAccessTabs`
    - apps/mobile/src/screens/AddFoodScreen.tsx contains `EntryMethodCards`
    - apps/mobile/src/screens/AddFoodScreen.tsx contains `navigate.*Detection`
    - apps/mobile/src/screens/AddFoodScreen.tsx contains `navigate.*BarcodeScan`
    - apps/mobile/src/screens/AddFoodScreen.tsx contains `navigate.*QuickAdd`
    - apps/mobile/src/navigation/RootNavigator.tsx contains `name="AddFood"` with `AddFoodScreen` component
    - apps/mobile/src/navigation/RootNavigator.tsx does NOT contain `AddFoodPlaceholder`
    - apps/mobile/src/screens/index.ts contains `AddFoodScreen`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>AddFoodScreen created with search bar, entry method cards, quick access tabs. Wired into RootNavigator. Camera/barcode/quick-add buttons navigate to existing screens. Barcode always visible (QA-06 fixed).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- AddFoodScreen renders search bar with camera, voice, barcode icons
- Entry method cards show 4 options in 2x2 grid
- Quick access tabs switch between Recent/Frequent/Favorites/Recipes
- Camera icon navigates to DetectionScreen
- Barcode icon navigates to BarcodeScanScreen
- Quick Add navigates to QuickAddScreen
</verification>

<success_criteria>
AddFoodScreen is the unified entry point for all food logging methods. Search bar has camera/voice/barcode icons (QA-06 fixed). Quick access tabs show personal food history. Entry method cards provide clear paths to each logging method. All navigation wired correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ux-redesign-diary-first-home-add-food-with-barcode-photo-voice-gallery-item-detail-bottom-sheet-long-press-context-menu-copy-move-meals/09-03-SUMMARY.md`
</output>
