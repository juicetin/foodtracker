---
phase: 09-ux-redesign
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/mobile/src/components/diary/MealGroupSection.tsx
  - apps/mobile/src/components/diary/MealGroupHeader.tsx
  - apps/mobile/src/components/diary/FoodItemCard.tsx
  - apps/mobile/src/components/diary/MacroSummaryHeader.tsx
  - apps/mobile/src/components/diary/DateNavigator.tsx
  - apps/mobile/src/components/diary/CalendarPicker.tsx
  - apps/mobile/src/components/diary/index.ts
  - apps/mobile/src/screens/DiaryHomeScreen.tsx
  - apps/mobile/src/navigation/MainTabNavigator.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - QA-03

must_haves:
  truths:
    - "Diary home screen shows remaining calories and P/C/F progress bars at top"
    - "Entries are grouped by meal type (Breakfast/Lunch/Dinner/Snacks) not time period"
    - "Each meal group can expand/collapse on tap"
    - "Date navigation works via arrows and swipe"
    - "Tapping date opens a calendar picker"
    - "Week overview bar shows which days have entries"
  artifacts:
    - path: "apps/mobile/src/screens/DiaryHomeScreen.tsx"
      provides: "Merged diary-first home screen"
      contains: "export default function DiaryHomeScreen"
    - path: "apps/mobile/src/components/diary/MealGroupSection.tsx"
      provides: "Collapsible meal group with entries"
      exports: ["MealGroupSection"]
    - path: "apps/mobile/src/components/diary/FoodItemCard.tsx"
      provides: "Compact food item row with tap and long-press"
      exports: ["FoodItemCard"]
    - path: "apps/mobile/src/components/diary/MacroSummaryHeader.tsx"
      provides: "Remaining cal + P/C/F progress bars"
      exports: ["MacroSummaryHeader"]
    - path: "apps/mobile/src/components/diary/DateNavigator.tsx"
      provides: "Arrows + date label + calendar tap"
      exports: ["DateNavigator"]
    - path: "apps/mobile/src/components/diary/CalendarPicker.tsx"
      provides: "Modal calendar picker for date selection"
      exports: ["CalendarPicker"]
  key_links:
    - from: "DiaryHomeScreen.tsx"
      to: "mealGroups.ts"
      via: "loadEntriesGroupedByMeal import"
      pattern: "loadEntriesGroupedByMeal"
    - from: "MealGroupSection.tsx"
      to: "FoodItemCard.tsx"
      via: "renders FoodItemCard for each entry"
      pattern: "FoodItemCard"
    - from: "MainTabNavigator.tsx"
      to: "DiaryHomeScreen.tsx"
      via: "Today tab component"
      pattern: "DiaryHomeScreen"
---

<objective>
Build the diary-first home screen with all diary UI components: meal group sections, food item cards, macro summary header, date navigator, and calendar picker. Wire into MainTabNavigator as the Today tab.

Purpose: This is the core screen users interact with daily. Replaces the separate Home + Diary screens with a unified diary-first view.
Output: DiaryHomeScreen and all supporting diary components, wired as the Today tab.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ux-redesign-diary-first-home-add-food-with-barcode-photo-voice-gallery-item-detail-bottom-sheet-long-press-context-menu-copy-move-meals/09-CONTEXT.md
@.planning/phases/09-ux-redesign-diary-first-home-add-food-with-barcode-photo-voice-gallery-item-detail-bottom-sheet-long-press-context-menu-copy-move-meals/09-UI-SPEC.md
@.planning/phases/09-ux-redesign-diary-first-home-add-food-with-barcode-photo-voice-gallery-item-detail-bottom-sheet-long-press-context-menu-copy-move-meals/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 service layer (mealGroups.ts) -->
export const MEAL_GROUPS = ['breakfast', 'lunch', 'dinner', 'snacks'] as const;
export type MealGroup = typeof MEAL_GROUPS[number];
export const MEAL_GROUP_CONFIG: Record<MealGroup, { label: string; icon: string }>;
export function loadEntriesGroupedByMeal(dateStr: string): Map<MealGroup, DiaryEntry[]>;
export function computeMealGroupTotals(entries: DiaryEntry[]): { calories: number; protein: number; carbs: number; fat: number };

<!-- From diaryQueries.ts -->
export interface DiaryEntry { id, timePeriod, mealType, totalCalories, totalProtein, totalCarbs, totalFat, notes, createdAt, photoUri, dishes }
export function loadWeekEntryPresence(centerDate: string): Map<string, number>;
export function computeDayTotals(entries: DiaryEntry[]): { calories, protein, carbs, fat };
export function getTodayDateStr(): string;
export function dateToStr(d: Date): string;
export function formatDateLabel(dateStr: string): string;

<!-- From existing StickyMacroHeader -->
interface MacroTotals { calories: number; protein: number; carbs: number; fat: number; }
interface StickyMacroHeaderProps { totals: MacroTotals; goals: MacroTotals; }

<!-- From types/index.ts (updated in Plan 01) -->
export type MainTabParamList = { Today: undefined; Add: undefined; Insights: undefined; Profile: undefined; };
export type RootStackParamList = { ...; AddFood: { mealType?: string }; ... };
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build diary interaction components</name>
  <files>
    apps/mobile/src/components/diary/MacroSummaryHeader.tsx,
    apps/mobile/src/components/diary/DateNavigator.tsx,
    apps/mobile/src/components/diary/MealGroupHeader.tsx,
    apps/mobile/src/components/diary/FoodItemCard.tsx,
    apps/mobile/src/components/diary/MealGroupSection.tsx,
    apps/mobile/src/components/diary/index.ts
  </files>
  <read_first>
    apps/mobile/src/components/diary/StickyMacroHeader.tsx,
    apps/mobile/src/components/diary/ExpandableEntryCard.tsx,
    apps/mobile/src/components/diary/TimePeriodSection.tsx,
    apps/mobile/src/components/diary/WeekOverviewBar.tsx,
    apps/mobile/src/components/diary/index.ts,
    apps/mobile/src/services/diary/mealGroups.ts,
    apps/mobile/src/services/diary/diaryQueries.ts,
    apps/mobile/src/store/usePreferencesStore.ts
  </read_first>
  <action>
1. **MacroSummaryHeader.tsx** — Rename/rewrite StickyMacroHeader with new design:
   - Props: `{ totals: MacroTotals; goals: MacroTotals }` (same interface)
   - Display remaining calories prominently: `goals.calories - totals.calories` in Display size (32px, weight 600, color #111827)
   - Label "Remaining" below cal number (14px, #9CA3AF)
   - Three horizontal progress bars for P/C/F:
     - Protein bar: fill #3B82F6 on track #EFF6FF, label "Protein" + value "Xg / Yg"
     - Carbs bar: fill #D97706 on track #FFFBEB, label "Carbs" + value "Xg / Yg"
     - Fat bar: fill #059669 on track #ECFDF5, label "Fat" + value "Xg / Yg"
   - Bar height: 8px, borderRadius 4px, clamp fill at 100%
   - Container: backgroundColor #FFFFFF, padding 16px, borderBottomWidth StyleSheet.hairlineWidth, borderBottomColor #E5E7EB
   - NO red/amber color for over-goal — just clamp bar at 100%. Only show red (#EF4444) text on the remaining cal number if negative (over goal)

2. **DateNavigator.tsx** — Extract from DiaryScreen:
   - Props: `{ dateStr: string; isToday: boolean; onPrevious: () => void; onNext: () => void; onDateTap: () => void }`
   - Render: left arrow (Ionicons chevron-back), date label (formatDateLabel), right arrow (chevron-forward)
   - Tap on date label calls onDateTap (opens CalendarPicker)
   - Arrow icons: color #374151, size 24, min touch target 44px
   - Date text: 20px, weight 600, color #111827
   - If isToday, show "Today" instead of full date text
   - Horizontal flexDirection row, justifyContent space-between, alignItems center

3. **MealGroupHeader.tsx** — Meal group header with expand/collapse:
   - Props: `{ mealGroup: MealGroup; subtotals: { calories, protein, carbs, fat }; expanded: boolean; onToggle: () => void; onLongPress: () => void; onAddFood: () => void }`
   - Render: icon (from MEAL_GROUP_CONFIG), label (from MEAL_GROUP_CONFIG), subtotal cal text (right side), expand/collapse chevron
   - Icon color: #6B7280, label: 16px weight 600 color #374151, subtotal: 14px color #6B7280
   - "+" button on right side: calls onAddFood, icon add-circle-outline, color #16A34A
   - Pressable: onPress=onToggle, onLongPress=onLongPress (will trigger MealGroupMenuSheet in Plan 04)
   - Use `delayLongPress={500}` to distinguish tap from long press
   - Min height 44px (touch target), paddingHorizontal 16px, paddingVertical 8px
   - Container backgroundColor: #FFFFFF

4. **FoodItemCard.tsx** — Compact food item row (replaces ExpandableEntryCard):
   - Props: `{ entry: DiaryEntry; onPress: () => void; onLongPress: () => void }`
   - NO more toggle states (QA-03 fix: remove third toggle). Just a simple card.
   - Render: photo thumbnail (40x40 borderRadius 8) or food emoji placeholder, dish name(s) (16px weight 600 #111827), cal value (16px weight 600 #111827), time logged (14px #9CA3AF)
   - Show first dish name from `entry.dishes[0]?.name || 'Food Entry'`
   - Macro pills row below name: P/C/F in small rounded pills (background #EFF6FF/#FFFBEB/#ECFDF5, text blue/amber/emerald, 14px)
   - Pressable: onPress opens detail sheet (Plan 04), onLongPress opens context menu (Plan 04)
   - Use `delayLongPress={500}`, set ref flag on long press to suppress subsequent onPress
   - backgroundColor #FFFFFF, padding 12px, marginHorizontal 16px, marginBottom 4px, borderRadius 12px
   - Light shadow: elevation 1, shadowOpacity 0.05

5. **MealGroupSection.tsx** — Collapsible meal group:
   - Props: `{ mealGroup: MealGroup; entries: DiaryEntry[]; onAddFood: (mealGroup: MealGroup) => void; onItemPress: (entryId: string) => void; onItemLongPress: (entry: DiaryEntry) => void; onHeaderLongPress: (mealGroup: MealGroup) => void }`
   - State: `expanded: boolean` (default true)
   - Render MealGroupHeader, then if expanded, render FoodItemCard for each entry
   - If expanded and no entries, show "+ Add Food" tappable text (14px, #16A34A)
   - Collapse/expand animation: use Reanimated withTiming 250ms for height
   - Container marginBottom 24px (lg spacing)

6. **Update `components/diary/index.ts`** barrel:
   - Export: MacroSummaryHeader, DateNavigator, MealGroupSection, MealGroupHeader, FoodItemCard, WeekOverviewBar
   - Keep WeekOverviewBar export (unchanged)
   - Remove: StickyMacroHeader, TimePeriodSection, ExpandableEntryCard, SearchBar exports (old components kept on disk for Plan 05 cleanup, just remove from barrel)
   - Do NOT export CalendarPicker from this barrel yet (added in Task 2)
  </action>
  <verify>
    <automated>cd /home/me/repos/personal/foodtracker/apps/mobile && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/components/diary/MacroSummaryHeader.tsx contains `export function MacroSummaryHeader`
    - apps/mobile/src/components/diary/MacroSummaryHeader.tsx contains `#3B82F6` (protein bar color)
    - apps/mobile/src/components/diary/MacroSummaryHeader.tsx contains `Remaining`
    - apps/mobile/src/components/diary/DateNavigator.tsx contains `export function DateNavigator`
    - apps/mobile/src/components/diary/DateNavigator.tsx contains `onDateTap`
    - apps/mobile/src/components/diary/MealGroupHeader.tsx contains `export function MealGroupHeader`
    - apps/mobile/src/components/diary/MealGroupHeader.tsx contains `delayLongPress`
    - apps/mobile/src/components/diary/FoodItemCard.tsx contains `export function FoodItemCard`
    - apps/mobile/src/components/diary/FoodItemCard.tsx contains `delayLongPress`
    - apps/mobile/src/components/diary/MealGroupSection.tsx contains `export function MealGroupSection`
    - apps/mobile/src/components/diary/index.ts contains `MacroSummaryHeader`
    - apps/mobile/src/components/diary/index.ts contains `MealGroupSection`
    - apps/mobile/src/components/diary/index.ts contains `FoodItemCard`
    - apps/mobile/src/components/diary/index.ts does NOT contain `StickyMacroHeader`
    - apps/mobile/src/components/diary/index.ts does NOT contain `TimePeriodSection`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>5 diary interaction components created (MacroSummaryHeader, DateNavigator, MealGroupHeader, FoodItemCard, MealGroupSection). Barrel index updated. Old components removed from exports.</done>
</task>

<task type="auto">
  <name>Task 2: Build CalendarPicker modal</name>
  <files>
    apps/mobile/src/components/diary/CalendarPicker.tsx,
    apps/mobile/src/components/diary/index.ts
  </files>
  <read_first>
    apps/mobile/src/components/diary/index.ts,
    apps/mobile/src/services/diary/diaryQueries.ts
  </read_first>
  <action>
1. **CalendarPicker.tsx** — Custom month-grid date picker modal:
   - Props: `{ visible: boolean; selectedDate: string; onSelect: (dateStr: string) => void; onDismiss: () => void }`
   - Use React Native Modal with transparent background + backdrop
   - State: `displayMonth: Date` (initialized from selectedDate, changes via month arrows)
   - Month/year header row: left arrow, "March 2026" text (16px weight 600), right arrow
   - Day-of-week header row: S M T W T F S (12px #9CA3AF)
   - Month grid: 7 columns, 4-6 rows depending on month
   - Each day cell: 40x40, centered number (14px)
     - Selected day: backgroundColor #16A34A, text #FFFFFF, borderRadius 20 (circle)
     - Today (if not selected): text underlined, color #16A34A
     - Other month days: color #D1D5DB (dimmed)
     - Normal days: color #111827
   - Tapping a day: calls onSelect with `dateToStr(tappedDate)`, then onDismiss
   - Modal container: centered, backgroundColor #FFFFFF, borderRadius 16px, padding 16px, elevation 8
   - Backdrop: backgroundColor rgba(0,0,0,0.3), onPress calls onDismiss
   - Generate month grid: compute first day of month, pad start with previous month's trailing days, fill 6 rows (42 cells)

2. **Update `components/diary/index.ts`** barrel:
   - Add CalendarPicker export
  </action>
  <verify>
    <automated>cd /home/me/repos/personal/foodtracker/apps/mobile && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/components/diary/CalendarPicker.tsx contains `export function CalendarPicker`
    - apps/mobile/src/components/diary/CalendarPicker.tsx contains `displayMonth`
    - apps/mobile/src/components/diary/CalendarPicker.tsx contains `#16A34A`
    - apps/mobile/src/components/diary/CalendarPicker.tsx contains `Modal`
    - apps/mobile/src/components/diary/CalendarPicker.tsx contains `onSelect`
    - apps/mobile/src/components/diary/index.ts contains `CalendarPicker`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>CalendarPicker modal created with custom month grid, month navigation, selected/today highlighting. Exported from diary barrel.</done>
</task>

<task type="auto">
  <name>Task 3: Create DiaryHomeScreen and wire as Today tab</name>
  <files>
    apps/mobile/src/screens/DiaryHomeScreen.tsx,
    apps/mobile/src/screens/index.ts,
    apps/mobile/src/navigation/MainTabNavigator.tsx
  </files>
  <read_first>
    apps/mobile/src/screens/DiaryScreen.tsx,
    apps/mobile/src/screens/HomeScreen.tsx,
    apps/mobile/src/components/diary/index.ts,
    apps/mobile/src/services/diary/mealGroups.ts,
    apps/mobile/src/services/diary/diaryQueries.ts,
    apps/mobile/src/store/usePreferencesStore.ts,
    apps/mobile/src/store/useFoodLogStore.ts,
    apps/mobile/src/navigation/MainTabNavigator.tsx
  </read_first>
  <action>
1. **Create `src/screens/DiaryHomeScreen.tsx`** — the merged diary-first home:
   - Import all new diary components from `../components/diary`: MacroSummaryHeader, DateNavigator, CalendarPicker, MealGroupSection, WeekOverviewBar
   - Import `loadEntriesGroupedByMeal, MEAL_GROUPS, type MealGroup` from `../services/diary/mealGroups`
   - Import `computeDayTotals, getTodayDateStr, loadWeekEntryPresence` from `../services/diary/diaryQueries`
   - Import `usePreferencesStore` for nutritionGoals
   - Import `Gesture, GestureDetector` from react-native-gesture-handler and `runOnJS` from react-native-reanimated

   State:
   - `selectedDate: string` (default getTodayDateStr())
   - `groupedEntries: Map<MealGroup, DiaryEntry[]>` (from loadEntriesGroupedByMeal)
   - `weekPresence: Map<string, number>`
   - `refreshing: boolean`
   - `calendarVisible: boolean`
   - `selectedEntryId: string | null` (for ItemDetailSheet — Plan 04 will use this)
   - `contextMenuEntry: DiaryEntry | null` (for ContextMenuSheet — Plan 04 will use this)
   - `menuMealGroup: MealGroup | null` (for MealGroupMenuSheet — Plan 04 will use this)

   Data loading (useFocusEffect):
   - Call `loadEntriesGroupedByMeal(selectedDate)` -> setGroupedEntries
   - Call `loadWeekEntryPresence(selectedDate)` -> setWeekPresence
   - Flatten all entries from groupedEntries to compute dayTotals via computeDayTotals

   Swipe gesture (carry from DiaryScreen):
   - Gesture.Pan().activeOffsetX([-20, 20]).failOffsetY([-10, 10])
   - onEnd: if translateX > 50 go previous day, if < -50 go next day
   - Use runOnJS for state updates

   Layout (top to bottom, inside SafeAreaView):
   - DateNavigator (arrows + date, onDateTap opens CalendarPicker)
   - MacroSummaryHeader (remaining cal + P/C/F bars)
   - WeekOverviewBar (unchanged, pass selectedDate + weekPresence + onSelectDate)
   - ScrollView with RefreshControl:
     - For each MEAL_GROUPS -> render MealGroupSection
       - onAddFood: navigate('AddFood', { mealType })
       - onItemPress: setSelectedEntryId (sheet opens in Plan 04)
       - onItemLongPress: setContextMenuEntry (menu opens in Plan 04)
       - onHeaderLongPress: setMenuMealGroup (menu opens in Plan 04)
   - Empty state when all groups have 0 entries: "No meals logged" / "Tap + to add your first meal, or take a photo of your food."
   - CalendarPicker modal (controlled by calendarVisible state)

   Styling:
   - Screen background: #F5F5F5
   - No header (headerShown: false in tab config)

   NOTE: Bottom sheet overlays (ItemDetailSheet, ContextMenuSheet, MealGroupMenuSheet) will be added by Plan 04. For now, onItemPress/onItemLongPress/onHeaderLongPress just set state but don't render sheets yet.

2. **Update `src/screens/index.ts`**:
   - Add `export { default as DiaryHomeScreen } from './DiaryHomeScreen';`

3. **Update `src/navigation/MainTabNavigator.tsx`**:
   - Replace the temporary DiaryScreen import on the Today tab with DiaryHomeScreen
   - `import { DiaryHomeScreen } from '../screens';`
   - Tab.Screen name="Today" component={DiaryHomeScreen}

   Export default as before.
  </action>
  <verify>
    <automated>cd /home/me/repos/personal/foodtracker/apps/mobile && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/screens/DiaryHomeScreen.tsx contains `export default function DiaryHomeScreen`
    - apps/mobile/src/screens/DiaryHomeScreen.tsx contains `loadEntriesGroupedByMeal`
    - apps/mobile/src/screens/DiaryHomeScreen.tsx contains `MacroSummaryHeader`
    - apps/mobile/src/screens/DiaryHomeScreen.tsx contains `MealGroupSection`
    - apps/mobile/src/screens/DiaryHomeScreen.tsx contains `DateNavigator`
    - apps/mobile/src/screens/DiaryHomeScreen.tsx contains `CalendarPicker`
    - apps/mobile/src/screens/DiaryHomeScreen.tsx contains `WeekOverviewBar`
    - apps/mobile/src/screens/DiaryHomeScreen.tsx contains `Gesture.Pan`
    - apps/mobile/src/screens/DiaryHomeScreen.tsx contains `#F5F5F5`
    - apps/mobile/src/screens/DiaryHomeScreen.tsx contains `selectedEntryId`
    - apps/mobile/src/navigation/MainTabNavigator.tsx contains `DiaryHomeScreen`
    - apps/mobile/src/screens/index.ts contains `DiaryHomeScreen`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>DiaryHomeScreen created with meal-type grouping, macro summary, date navigation, calendar picker, swipe gestures. Wired as Today tab in MainTabNavigator.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Today tab renders DiaryHomeScreen with meal groups (Breakfast/Lunch/Dinner/Snacks)
- MacroSummaryHeader shows remaining calories and P/C/F progress bars
- DateNavigator supports arrow navigation and date tap for calendar
- CalendarPicker opens as modal when tapping date
- Swipe gesture navigates between days
- FoodItemCard supports onPress and onLongPress with 500ms delay
</verification>

<success_criteria>
DiaryHomeScreen is the home tab, displaying meal-type grouped entries with macro summary header, date navigation, calendar picker, and week overview bar. All 6 new diary components built with UI-SPEC colors/spacing. Swipe navigation works. Old time-period grouping replaced by meal-type grouping.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ux-redesign-diary-first-home-add-food-with-barcode-photo-voice-gallery-item-detail-bottom-sheet-long-press-context-menu-copy-move-meals/09-02-SUMMARY.md`
</output>
