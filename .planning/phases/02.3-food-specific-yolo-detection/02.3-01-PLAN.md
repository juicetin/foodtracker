---
phase: 02.3-food-specific-yolo-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - training/export_ggcd_detect.py
  - apps/mobile/assets/models/detect.tflite
  - apps/mobile/assets/models/labels_detect.json
  - apps/mobile/assets/models/model_manifest.json
  - apps/mobile/src/services/detection/constants.ts
autonomous: true
requirements: [ML-02]

must_haves:
  truths:
    - "GGCD YOLOv8n TFLite model is bundled as the detection model"
    - "241 food-specific class labels are available as a JSON asset"
    - "Model manifest reflects GGCD YOLOv8n with 241 classes"
    - "Detection constants reference GGCD class names instead of COCO names"
  artifacts:
    - path: "training/export_ggcd_detect.py"
      provides: "YOLOv8n PyTorch to TFLite export script"
    - path: "apps/mobile/assets/models/detect.tflite"
      provides: "GGCD YOLOv8n TFLite detection model (241 food classes)"
    - path: "apps/mobile/assets/models/labels_detect.json"
      provides: "241 food-specific class names JSON"
    - path: "apps/mobile/assets/models/model_manifest.json"
      provides: "Updated manifest with GGCD detection model metadata"
    - path: "apps/mobile/src/services/detection/constants.ts"
      provides: "DETECT_CLASS_NAMES (241 food names), removed COCO_CLASS_NAMES and COCO_FOOD_CLASS_IDS"
  key_links:
    - from: "apps/mobile/src/services/detection/constants.ts"
      to: "apps/mobile/assets/models/labels_detect.json"
      via: "require() import for class names"
      pattern: "require.*labels_detect\\.json"
    - from: "apps/mobile/assets/models/model_manifest.json"
      to: "apps/mobile/assets/models/detect.tflite"
      via: "file reference in manifest"
      pattern: "detect\\.tflite"
---

<objective>
Convert the GGCD YOLOv8n PyTorch model to TFLite, deploy it as the bundled detection model, and update constants to reference 241 food-specific class names instead of 80 COCO classes.

Purpose: The current YOLO11n COCO detector only recognizes 10 food classes out of 80 total, requiring post-hoc filtering. GGCD YOLOv8n is trained specifically on 241 food classes from global cuisines, enabling direct multi-dish detection without filtering.

Output: Exported TFLite model, labels JSON asset, updated manifest, updated constants.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-food-specific-yolo-detection/02.3-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From apps/mobile/src/services/detection/constants.ts (after Phase 2.2 -- will be modified):
```typescript
export const DETECT_INPUT_SIZE = 640;
export const CLASSIFY_INPUT_SIZE = 224;
export const COCO_CLASS_NAMES: string[];          // 80 COCO names -- TO BE REPLACED
export const COCO_FOOD_CLASS_IDS: Set<number>;    // 10 food class IDs -- TO BE REMOVED
export const CLASSIFY_CLASS_NAMES: string[];       // 335 EfficientNet-Lite0 food names
export const IMAGENET_MEAN: [number, number, number];
export const IMAGENET_STD: [number, number, number];
```

From apps/mobile/assets/models/model_manifest.json (after Phase 2.2):
The manifest will have a `yolo-detect-v1` entry for YOLO11n COCO that needs updating.

GGCD model source:
- PyTorch weights: `/home/me/media/foodtracker-ml/models/ggcd-yolo/yolov8n_ggcd.pt` (6.7MB)
- Class names: `/home/me/media/foodtracker-ml/models/ggcd-yolo/ggcd_classes.json` with format: `{ "source": "GGCD/YOLOv8n", "count": 241, "classNames": ["achichuk", ..., "zucchini"] }`

Existing export script pattern: `training/export_mobile.py` uses ultralytics YOLO.export(format="tflite", ...) with FP16 quantisation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export GGCD YOLOv8n to TFLite and deploy assets</name>
  <files>
    training/export_ggcd_detect.py
    apps/mobile/assets/models/detect.tflite
    apps/mobile/assets/models/labels_detect.json
    apps/mobile/assets/models/model_manifest.json
  </files>
  <action>
**Create `training/export_ggcd_detect.py`** -- a focused export script for the GGCD YOLOv8n detection model.

This is a single-purpose script (not modifying the existing `export_mobile.py` which handles the 3-stage pipeline). The script:

1. Loads the GGCD YOLOv8n PyTorch weights from `/home/me/media/foodtracker-ml/models/ggcd-yolo/yolov8n_ggcd.pt` using ultralytics YOLO.
2. Exports to TFLite with FP16 quantisation (`format="tflite"`, `half=True`, `imgsz=640`).
   - Per existing convention: `nms=False` (NMS done in JavaScript).
   - Use FP16 first (same as YOLO11n export). INT8 can be explored later if size is a concern.
   - If ultralytics export fails (some YOLOv8 versions have issues), fall back to ONNX -> onnx2tf pipeline.
3. Validates the exported TFLite:
   - Load with ai-edge-litert (or tflite_runtime as fallback).
   - Verify input shape is `[1, 640, 640, 3]` (NHWC) and dtype is float32.
   - Verify output shape contains `245` (4 bbox + 241 classes) in one dimension.
   - Log input/output details.
4. Copies the validated TFLite to `apps/mobile/assets/models/detect.tflite` (overwriting the YOLO11n COCO model).
5. Creates `apps/mobile/assets/models/labels_detect.json` from the GGCD classes file:
   ```json
   {
     "source": "GGCD/YOLOv8n",
     "count": 241,
     "classNames": ["achichuk", "airan-katyk", ..., "zucchini"]
   }
   ```
   Copy directly from `/home/me/media/foodtracker-ml/models/ggcd-yolo/ggcd_classes.json`.

Script should accept `--weights`, `--output-dir`, `--validate` arguments similar to `export_mobile.py`.
Default `--weights` to `/home/me/media/foodtracker-ml/models/ggcd-yolo/yolov8n_ggcd.pt`.
Default `--output-dir` to `apps/mobile/assets/models/`.

**Run the export script** after creating it:
```bash
cd /home/me/repos/personal/foodtracker && python training/export_ggcd_detect.py --validate
```

If the export fails due to Python/TF version issues (CONTEXT mentions Docker may be needed for onnx2tf), try these fallbacks in order:
1. `ultralytics` direct export (preferred)
2. Export to ONNX first (`model.export(format="onnx", imgsz=640)`), then use `onnx2tf` CLI
3. If all else fails, use the existing `export_mobile.py` with `--detect-weights /home/me/media/foodtracker-ml/models/ggcd-yolo/yolov8n_ggcd.pt`

**Update `apps/mobile/assets/models/model_manifest.json`:**

Update the `yolo-detect-v1` entry (or whatever the detect entry is named after Phase 2.2):
- `"id": "ggcd-detect-v1"`
- `"stage": "detect"`
- `"version": "1.0.0"`
- `"file": "detect.tflite"`
- `"sizeBytes":` (actual file size from the exported model)
- `"format": "tflite"`
- `"quantisation": "fp16"` (or "int8" if INT8 export succeeds)
- `"inputSize": 640`
- `"numClasses": 241`
- `"inputNormalization": "float32_0_1"`
- `"inputDtype": "float32"`
- `"outputShape": [1, 245, 8400]`
- `"name": "GGCD YOLOv8n (241-Class Food Detection)"`
- `"description": "YOLOv8n trained on Global Gastronomic Culinary Dataset. 241 food-specific classes spanning Central Asian, South Asian, East Asian, Middle Eastern, European, and American cuisines. NMS performed in JavaScript."`

Also remove `apps/mobile/assets/models/labels_coco.json` -- no longer needed since detection uses GGCD labels directly.
  </action>
  <verify>
    <automated>test -f apps/mobile/assets/models/detect.tflite && test -f apps/mobile/assets/models/labels_detect.json && python3 -c "import json; d=json.load(open('apps/mobile/assets/models/labels_detect.json')); assert d['count']==241; assert len(d['classNames'])==241; print('PASS: 241 classes')" && ! test -f apps/mobile/assets/models/labels_coco.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - detect.tflite is GGCD YOLOv8n (not YOLO11n COCO)
    - labels_detect.json has exactly 241 food-specific class names
    - labels_coco.json removed (no longer needed)
    - model_manifest.json updated with GGCD detection model metadata
    - Export script exists and is reproducible
  </done>
</task>

<task type="auto">
  <name>Task 2: Update constants.ts -- replace COCO names with GGCD food names</name>
  <files>
    apps/mobile/src/services/detection/constants.ts
  </files>
  <action>
**Update `apps/mobile/src/services/detection/constants.ts`:**

1. Add a `require()` import for the new detection labels file at the top:
   ```typescript
   // eslint-disable-next-line @typescript-eslint/no-var-requires
   const detectLabels = require('../../../assets/models/labels_detect.json');
   ```

2. Remove the inline `COCO_CLASS_NAMES` array definition (the 80-element string array with "person", "bicycle", etc.).

3. Remove the `COCO_FOOD_CLASS_IDS` Set export entirely. After this phase, all 241 detection classes are food -- no filtering needed.

4. Remove the `require()` import for `labels_coco.json` if it exists (it may be inline or from a file).

5. Add new export for the 241 GGCD food class names:
   ```typescript
   /**
    * GGCD YOLOv8n detection class names (241 food-specific entries).
    * Index-aligned with model output class indices.
    * Trained on Global Gastronomic Culinary Dataset.
    */
   export const DETECT_CLASS_NAMES: string[] = detectLabels.classNames;
   ```

6. Keep all other exports unchanged:
   - `DETECT_INPUT_SIZE = 640` (still 640x640 input)
   - `CLASSIFY_INPUT_SIZE = 224` (from Phase 2.2)
   - `CLASSIFY_CLASS_NAMES` (from Phase 2.2)
   - `IMAGENET_MEAN`, `IMAGENET_STD` (from Phase 2.2)

The result is that `constants.ts` exports `DETECT_CLASS_NAMES` (241 food names for detection) and `CLASSIFY_CLASS_NAMES` (335 food names for classification) -- both food-specific, no COCO references remaining.

NOTE: After Phase 2.2, `COCO_CLASS_NAMES` and `COCO_FOOD_CLASS_IDS` will still be referenced by `inferenceRouter.ts` and `DetectionScreen.tsx`. Those references are updated in Plan 02. Here we just update the constants file itself. If there are TypeScript compile errors from removed exports, that is expected and will be resolved in Plan 02.
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const d = require('./assets/models/labels_detect.json'); console.log('Detect labels:', d.classNames.length)" && grep -c 'DETECT_CLASS_NAMES' src/services/detection/constants.ts && grep -c 'COCO_CLASS_NAMES\|COCO_FOOD_CLASS_IDS' src/services/detection/constants.ts | xargs -I{} test {} -eq 0 && echo "PASS" || echo "PARTIAL -- check COCO removal"</automated>
  </verify>
  <done>
    - constants.ts exports DETECT_CLASS_NAMES (241 GGCD food names)
    - COCO_CLASS_NAMES removed from constants.ts
    - COCO_FOOD_CLASS_IDS removed from constants.ts
    - DETECT_INPUT_SIZE unchanged at 640
    - All Phase 2.2 exports preserved (CLASSIFY_CLASS_NAMES, IMAGENET_MEAN, IMAGENET_STD)
  </done>
</task>

</tasks>

<verification>
1. `ls -la apps/mobile/assets/models/detect.tflite` shows a valid TFLite file (expected ~6-7MB for FP16 or ~3-4MB for INT8)
2. `python3 -c "import json; d=json.load(open('apps/mobile/assets/models/labels_detect.json')); print(d['count'], d['classNames'][:5])"` shows 241 food names
3. `grep -c 'COCO' apps/mobile/src/services/detection/constants.ts` returns 0
4. `grep 'DETECT_CLASS_NAMES' apps/mobile/src/services/detection/constants.ts` shows the new export
5. `python3 -c "import json; m=json.load(open('apps/mobile/assets/models/model_manifest.json')); d=[x for x in m['models'] if x['stage']=='detect'][0]; print(d['id'], d['numClasses'])"` shows ggcd-detect-v1 with 241 classes
</verification>

<success_criteria>
- GGCD YOLOv8n TFLite deployed as the bundled detection model
- 241 food-specific class names available as JSON asset and TypeScript constant
- No COCO references remain in constants.ts
- Model manifest accurately describes the new detection model
- Export script is reproducible for future re-exports
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-food-specific-yolo-detection/02.3-01-SUMMARY.md`
</output>
