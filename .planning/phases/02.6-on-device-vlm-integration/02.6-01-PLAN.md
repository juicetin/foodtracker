---
phase: 02.6-on-device-vlm-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/jest.config.js
  - apps/mobile/__mocks__/llama.rn.ts
  - apps/mobile/src/services/vlm/vlmTypes.ts
  - apps/mobile/src/services/vlm/ramDetector.ts
  - apps/mobile/src/services/vlm/__tests__/ramDetector.test.ts
autonomous: true
requirements:
  - DET-02
must_haves:
  truths:
    - "llama.rn and expo-device are installed and Expo plugin config compiles"
    - "VLM type contracts are defined and importable"
    - "RAM-based tier selection returns correct tier for each memory bracket"
    - "Jest can run tests that import llama.rn without native module errors"
  artifacts:
    - path: "apps/mobile/src/services/vlm/vlmTypes.ts"
      provides: "VLM type contracts (VlmTier, VlmTierConfig, VlmFoodResult, VlmDish)"
      exports: ["VlmTier", "VlmTierConfig", "VLM_TIER_CONFIG", "VlmFoodResult", "VlmDish", "FOOD_IDENTIFICATION_SCHEMA"]
    - path: "apps/mobile/src/services/vlm/ramDetector.ts"
      provides: "Device RAM detection and VLM tier selection"
      exports: ["detectVlmTier", "getVlmTierConfig"]
    - path: "apps/mobile/__mocks__/llama.rn.ts"
      provides: "Jest mock for llama.rn native module"
  key_links:
    - from: "apps/mobile/src/services/vlm/ramDetector.ts"
      to: "expo-device"
      via: "Device.totalMemory"
      pattern: "Device\\.totalMemory"
    - from: "apps/mobile/src/services/vlm/ramDetector.ts"
      to: "apps/mobile/src/services/vlm/vlmTypes.ts"
      via: "VLM_TIER_CONFIG import"
      pattern: "VLM_TIER_CONFIG"
---

<objective>
Install llama.rn and expo-device dependencies, configure Expo plugins, create VLM type contracts and RAM-based tier selection, and set up Jest mocks for llama.rn.

Purpose: Establishes the dependency foundation and type contracts that all subsequent VLM plans build on. Without this, no VLM code can compile or be tested.
Output: Installable deps, compilable types, passing tier selection tests, Jest mock for llama.rn.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.6-on-device-vlm-integration/02.6-CONTEXT.md
@.planning/phases/02.6-on-device-vlm-integration/02.6-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From apps/mobile/src/services/packs/types.ts:
```typescript
export interface PackEntry {
  id: string;
  name: string;
  type: 'nutrition' | 'model' | 'knowledge-graph';
  // ... will be extended with 'vlm' type
}
export interface InstalledPack {
  id: string;
  type: 'nutrition' | 'model' | 'knowledge-graph';
  filePath: string;
  // ... will gain mmprojFilePath
}
```

From apps/mobile/app.json (current plugins):
```json
"plugins": [
  "expo-localization",
  ["react-native-fast-tflite", { "enableCoreMLDelegate": true, "enableAndroidGpuLibraries": true }],
  "expo-asset"
]
```

From apps/mobile/jest.config.js (current moduleNameMapper):
```javascript
moduleNameMapper: {
  '^@op-engineering/op-sqlite$': '<rootDir>/__mocks__/@op-engineering/op-sqlite.ts',
  '\\.tflite$': '<rootDir>/__mocks__/tfliteAsset.js',
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install deps, configure plugins, create VLM types and Jest mock</name>
  <files>apps/mobile/package.json, apps/mobile/app.json, apps/mobile/jest.config.js, apps/mobile/__mocks__/llama.rn.ts, apps/mobile/src/services/vlm/vlmTypes.ts</files>
  <behavior>
    - VlmTier type is 'budget' | 'mid' | 'high' | 'none'
    - VLM_TIER_CONFIG maps each tier to model file names, sizes, and RAM estimates
    - VlmFoodResult has dishes array with name, cuisine, ingredients, portion_hint
    - FOOD_IDENTIFICATION_SCHEMA is a valid JSON schema for grammar-constrained output
    - llama.rn mock exports initLlama that returns a context with completion and release methods
  </behavior>
  <action>
    1. Run `cd apps/mobile && npm install llama.rn expo-device` to install dependencies.

    2. Update `apps/mobile/app.json` plugins array -- add llama.rn config plugin AFTER the existing plugins:
       ```json
       ["llama.rn", { "enableEntitlements": true, "entitlementsProfile": "production", "forceCxx20": true, "enableOpenCL": true }],
       "expo-device"
       ```

    3. Create `apps/mobile/src/services/vlm/vlmTypes.ts` with:
       - `VlmTier = 'budget' | 'mid' | 'high' | 'none'`
       - `VlmTierConfig` interface: modelId, modelFile, mmprojFile, modelSize, mmprojSize, totalDownload, runtimeRam (all numbers except modelId/modelFile/mmprojFile which are strings)
       - `VLM_TIER_CONFIG: Record<Exclude<VlmTier, 'none'>, VlmTierConfig>` with exact values from RESEARCH.md:
         - budget: SmolVLM-256M-Instruct-Q8_0.gguf (175MB model, 190MB mmproj)
         - mid: SmolVLM-500M-Instruct-Q8_0.gguf (437MB model, 109MB mmproj)
         - high: SmolVLM2-2.2B-Instruct-Q4_K_M.gguf (1.11GB model, 190MB mmproj)
       - `VlmDish` interface: name (string), cuisine (string), ingredients (string[]), portion_hint? (string)
       - `VlmFoodResult` interface: dishes (VlmDish[])
       - `FOOD_IDENTIFICATION_SCHEMA` const: JSON schema object matching the VlmFoodResult shape, with strict: true, for grammar-constrained output. Follow the schema structure from RESEARCH.md code example.

    4. Create `apps/mobile/__mocks__/llama.rn.ts`:
       - Export `initLlama` as a jest.fn() that returns a Promise resolving to a mock context object with:
         - `initMultimodal`: jest.fn() returning Promise<void>
         - `completion`: jest.fn() returning Promise<{ text: '{"dishes":[]}' }>
         - `release`: jest.fn() returning Promise<void>
       - Export `LlamaContext` as a type (can be empty interface for mock)

    5. Update `apps/mobile/jest.config.js` moduleNameMapper -- add entry:
       `'^llama\\.rn$': '<rootDir>/__mocks__/llama.rn.ts'`
       Place it after the existing op-sqlite and tflite entries.
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const t = require('./src/services/vlm/vlmTypes'); console.log(Object.keys(t.VLM_TIER_CONFIG)); console.log(t.FOOD_IDENTIFICATION_SCHEMA.name)" 2>&1 | grep -q "budget" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>llama.rn and expo-device in package.json, app.json plugins updated, vlmTypes.ts exports VLM_TIER_CONFIG and FOOD_IDENTIFICATION_SCHEMA, llama.rn mock exists, jest.config.js maps llama.rn to mock</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RAM detector with tier selection logic and tests</name>
  <files>apps/mobile/src/services/vlm/ramDetector.ts, apps/mobile/src/services/vlm/__tests__/ramDetector.test.ts</files>
  <behavior>
    - detectVlmTier() returns 'none' when Device.totalMemory is null
    - detectVlmTier() returns 'none' for less than 4GB RAM
    - detectVlmTier() returns 'budget' for 4-5.99GB RAM
    - detectVlmTier() returns 'mid' for 6-7.99GB RAM
    - detectVlmTier() returns 'high' for 8GB+ RAM
    - getVlmTierConfig() returns null for 'none' tier
    - getVlmTierConfig() returns correct VlmTierConfig for valid tiers
  </behavior>
  <action>
    1. Create `apps/mobile/src/services/vlm/ramDetector.ts`:
       - Import `* as Device from 'expo-device'`
       - Import `VlmTier, VlmTierConfig, VLM_TIER_CONFIG` from `./vlmTypes`
       - `detectVlmTier(): VlmTier` -- reads Device.totalMemory (bytes), converts to GB, returns tier per thresholds: >=8 -> 'high', >=6 -> 'mid', >=4 -> 'budget', else 'none'. Return 'none' if totalMemory is null.
       - `getVlmTierConfig(): VlmTierConfig | null` -- calls detectVlmTier(), returns VLM_TIER_CONFIG[tier] or null for 'none'.

    2. Create `apps/mobile/src/services/vlm/__tests__/ramDetector.test.ts`:
       - Mock `expo-device` with jest.mock: mock totalMemory as a getter so it can be changed per test
       - Test cases for each RAM bracket: null -> 'none', 3GB -> 'none', 4GB -> 'budget', 6GB -> 'mid', 8GB -> 'high', 12GB -> 'high'
       - Test getVlmTierConfig returns null for 'none', returns config with correct modelFile for 'budget'

    Note: expo-device mock -- use `jest.spyOn` or `Object.defineProperty` on the Device module to set totalMemory per test. The existing codebase mocks op-sqlite similarly.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest --testPathPattern='ramDetector' --no-coverage -x 2>&1 | tail -5</automated>
  </verify>
  <done>ramDetector.ts exports detectVlmTier and getVlmTierConfig, all 7+ test cases pass covering each RAM bracket and edge cases</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx jest --testPathPattern='ramDetector' --no-coverage -x` -- all tests pass
- `cd apps/mobile && cat node_modules/llama.rn/package.json | head -3` -- llama.rn installed
- `cd apps/mobile && node -e "require('./src/services/vlm/vlmTypes')"` -- types importable
</verification>

<success_criteria>
- llama.rn and expo-device installed in package.json
- app.json has llama.rn and expo-device plugin entries
- VLM type contracts (VlmTier, VLM_TIER_CONFIG, VlmFoodResult, FOOD_IDENTIFICATION_SCHEMA) are defined and importable
- RAM detector correctly maps device memory to VLM tier with tests passing
- Jest mock for llama.rn prevents native module errors in test environment
</success_criteria>

<output>
After completion, create `.planning/phases/02.6-on-device-vlm-integration/02.6-01-SUMMARY.md`
</output>
