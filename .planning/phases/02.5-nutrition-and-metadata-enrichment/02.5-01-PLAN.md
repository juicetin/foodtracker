---
phase: 02.5-nutrition-and-metadata-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - knowledge-graph/build_off_nutrition_db.py
  - knowledge-graph/build_nutrition5k_db.py
  - knowledge-graph/tests/test_off_build.py
  - knowledge-graph/tests/test_nutrition5k_build.py
  - knowledge-graph/requirements.txt
autonomous: true
requirements: [ML-04]

must_haves:
  truths:
    - "Open Food Facts products with complete nutrition data are filtered and stored in a SQLite database matching the published nutrition pack schema"
    - "Nutrition5k per-dish calorie/macro data is downloaded from HuggingFace and stored in a SQLite database matching the published nutrition pack schema"
    - "Both databases have FTS5 indexes for food name search"
    - "Both databases use standard USDA FDC nutrient IDs (1008=Energy, 1003=Protein, 1005=Carbs, 1004=Fat) for cross-DB compatibility"
  artifacts:
    - path: "knowledge-graph/build_off_nutrition_db.py"
      provides: "Open Food Facts CSV-to-SQLite pipeline with food product filtering"
      min_lines: 80
    - path: "knowledge-graph/build_nutrition5k_db.py"
      provides: "Nutrition5k HuggingFace-to-SQLite pipeline with dish-level macros"
      min_lines: 60
    - path: "knowledge-graph/tests/test_off_build.py"
      provides: "Tests for OFF build pipeline"
      min_lines: 30
    - path: "knowledge-graph/tests/test_nutrition5k_build.py"
      provides: "Tests for Nutrition5k build pipeline"
      min_lines: 30
  key_links:
    - from: "knowledge-graph/build_off_nutrition_db.py"
      to: "nutrition pack schema"
      via: "Same SQL schema as build_usda_db.py (foods, nutrients, food_nutrients, food_portions, foods_fts)"
      pattern: "CREATE TABLE foods.*fdc_id INTEGER PRIMARY KEY"
    - from: "knowledge-graph/build_nutrition5k_db.py"
      to: "nutrition pack schema"
      via: "Same SQL schema as build_usda_db.py"
      pattern: "CREATE TABLE foods.*fdc_id INTEGER PRIMARY KEY"
---

<objective>
Build Python data pipelines that process Open Food Facts (4.4M products) and Nutrition5k (3,260 dishes) into SQLite nutrition pack databases matching the published schema.

Purpose: The app needs enriched nutrition data beyond USDA and regional databases. OFF provides packaged food nutrition (4.4M products) and Nutrition5k provides ground-truth per-dish calorie data for common cafeteria meals. Both must conform to the existing nutrition pack schema so they work with NutritionService and RegionalResolver.

Output: Two SQLite databases (off-nutrition.db, nutrition5k.db) ready for bundling or download as nutrition packs.
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-nutrition-and-metadata-enrichment/02.5-CONTEXT.md
@.planning/phases/01-infrastructure-data-foundation/01-02-SUMMARY.md
@.planning/phases/01-infrastructure-data-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Existing nutrition pack schema that both new pipelines MUST match -->

From knowledge-graph/build_usda_db.py (lines 33-67):
```sql
CREATE TABLE foods (
    fdc_id INTEGER PRIMARY KEY,
    description TEXT NOT NULL,
    food_category TEXT,
    data_type TEXT,
    publication_date TEXT
);

CREATE TABLE nutrients (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    unit TEXT NOT NULL,
    nutrient_nbr TEXT
);

CREATE TABLE food_nutrients (
    food_id INTEGER REFERENCES foods(fdc_id),
    nutrient_id INTEGER REFERENCES nutrients(id),
    amount REAL NOT NULL,
    PRIMARY KEY (food_id, nutrient_id)
);

CREATE TABLE food_portions (
    id INTEGER PRIMARY KEY,
    food_id INTEGER REFERENCES foods(fdc_id),
    portion_description TEXT,
    gram_weight REAL,
    modifier TEXT
);

CREATE VIRTUAL TABLE foods_fts USING fts5(
    description,
    food_category,
    content=foods,
    content_rowid=fdc_id
);
```

Standard nutrient IDs (from nutritionSchema.ts):
```typescript
ENERGY: 1008, PROTEIN: 1003, CARBS: 1005, FAT: 1004, FIBER: 1079
```

Existing build pipeline pattern (from build_regional_db.py):
- argparse CLI with --source, --input-dir, --output flags
- STANDARD_NUTRIENTS list with USDA FDC nutrient IDs
- source-specific loader -> common schema insert -> FTS5 index -> ANALYZE -> VACUUM
- conn.commit() before VACUUM (cannot VACUUM inside transaction)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Open Food Facts CSV-to-SQLite pipeline</name>
  <files>knowledge-graph/build_off_nutrition_db.py, knowledge-graph/tests/test_off_build.py, knowledge-graph/requirements.txt</files>
  <behavior>
    - Test 1: Builds valid SQLite from mock OFF CSV data with all required tables (foods, nutrients, food_nutrients, food_portions, foods_fts)
    - Test 2: Filters out products missing nutrition data (no energy_kcal_100g)
    - Test 3: Filters out non-food products (categories containing "beauty", "cleaning", etc.)
    - Test 4: Maps OFF nutrients to standard USDA FDC nutrient IDs (energy->1008, proteins->1003, carbohydrates->1005, fat->1004, fiber->1079)
    - Test 5: FTS5 search returns matching products by name
    - Test 6: Uses synthetic sequential fdc_id (OFF product codes are strings/barcodes, not integers)
  </behavior>
  <action>
    Create build_off_nutrition_db.py that:
    1. Reads the compressed OFF CSV (openfoodfacts-products.csv.gz) at the path specified via --input flag. The file is at ~/media/foodtracker-ml/nutrition-data/openfoodfacts/openfoodfacts-products.csv.gz (1.1GB compressed). Use Python csv.reader with gzip.open to stream rows without loading all into memory.
    2. Filters to food products only: skip rows where categories contain non-food markers (beauty, cosmetic, cleaning, pet, baby_care). Keep only rows with completeness_score >= 0.5 OR (product_name not empty AND energy_kcal_100g is a valid number > 0).
    3. Extracts per-100g nutrition: energy_kcal_100g -> nutrient 1008, proteins_100g -> 1003, carbohydrates_100g -> 1005, fat_100g -> 1004, fiber_100g -> 1079, sodium_100g -> 1093, sugars_100g -> 2000, saturated-fat_100g -> 1258.
    4. Assigns synthetic sequential fdc_id starting at 9000001 (high range to avoid USDA collision). Stores original OFF barcode/code in data_type field as "off:{code}".
    5. Uses food_category from OFF's "main_category_en" or "categories_en" (first category).
    6. Sets data_type to "open_food_facts" and publication_date to current date.
    7. Inserts portion data: if serving_size field exists and has gram weight, insert as a food_portion row.
    8. Builds FTS5 index, runs ANALYZE, VACUUM (commit before VACUUM per established pattern).
    9. CLI: `python build_off_nutrition_db.py --input /path/to/products.csv.gz --output off-nutrition.db [--max-rows N]` where --max-rows limits rows for testing.
    10. Logs progress every 100,000 rows.

    Add polars to knowledge-graph/requirements.txt for fast CSV reading. Actually, use standard csv + gzip for consistency with existing pipelines (no polars dependency). Stream rows one at a time.

    Create test_off_build.py with fixture CSV data (small inline CSV string) testing all behaviors above.
  </action>
  <verify>
    <automated>cd /home/me/repos/personal/foodtracker/knowledge-graph && python -m pytest tests/test_off_build.py -x -v</automated>
  </verify>
  <done>build_off_nutrition_db.py produces a valid nutrition pack SQLite from OFF CSV data, all tests pass, script handles 1.1GB compressed input via streaming</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Nutrition5k HuggingFace-to-SQLite pipeline</name>
  <files>knowledge-graph/build_nutrition5k_db.py, knowledge-graph/tests/test_nutrition5k_build.py</files>
  <behavior>
    - Test 1: Builds valid SQLite from mock Nutrition5k data with all required tables
    - Test 2: Maps dish-level calories, fat, carbs, protein to standard USDA nutrient IDs
    - Test 3: Uses dish description as food name
    - Test 4: FTS5 search returns matching dishes by name
    - Test 5: Handles missing or zero nutrient values gracefully (inserts 0.0, not null)
    - Test 6: Assigns synthetic sequential fdc_id starting at 8000001 (separate range from OFF)
  </behavior>
  <action>
    Create build_nutrition5k_db.py that:
    1. Downloads the mmathys/food-nutrients dataset from HuggingFace using the `datasets` library (already in training/.venv). Or if a --input-dir flag points to a local directory with the data, read from there.
    2. The dataset has columns: dish_id, total_calories, total_fat, total_carb, total_protein, and per-ingredient breakdowns. We use the total_* fields for dish-level nutrition.
    3. Assigns synthetic sequential fdc_id starting at 8000001.
    4. Maps: total_calories -> nutrient 1008 (per 100g normalize IF serving weight available, otherwise store as-is with portion entry for "1 plate"), total_fat -> 1004, total_carb -> 1005, total_protein -> 1003.
    5. Sets data_type to "nutrition5k" and food_category to "prepared dish".
    6. Adds a food_portion entry: "1 plate" with gram_weight from the dataset (or 400g default for cafeteria plates if not available).
    7. Builds FTS5 index, ANALYZE, VACUUM.
    8. CLI: `python build_nutrition5k_db.py [--input-dir /path/to/data] --output nutrition5k.db`
    9. If using HuggingFace datasets library, install it in knowledge-graph venv. Otherwise, the data can be downloaded as CSV/Parquet.

    Create test_nutrition5k_build.py with mock data testing all behaviors above. Use a small dict or JSON fixture rather than real HuggingFace download in tests.
  </action>
  <verify>
    <automated>cd /home/me/repos/personal/foodtracker/knowledge-graph && python -m pytest tests/test_nutrition5k_build.py -x -v</automated>
  </verify>
  <done>build_nutrition5k_db.py produces a valid nutrition pack SQLite from Nutrition5k data, all tests pass, script can download from HuggingFace or read local data</done>
</task>

</tasks>

<verification>
Both build scripts produce SQLite databases conforming to the published nutrition pack schema:
1. `python build_off_nutrition_db.py --input test.csv.gz --output /tmp/off-test.db --max-rows 100` completes successfully
2. `python build_nutrition5k_db.py --input-dir /tmp/n5k-test --output /tmp/n5k-test.db` completes successfully
3. Both databases pass schema validation (foods, nutrients, food_nutrients, food_portions, foods_fts tables present)
4. Both use standard USDA FDC nutrient IDs
</verification>

<success_criteria>
- OFF pipeline filters 4.4M products to food-only items with nutrition data
- Nutrition5k pipeline processes 3,260 dishes with per-dish macros
- Both produce valid nutrition pack SQLite databases
- All tests pass
- Scripts committed to knowledge-graph/
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-nutrition-and-metadata-enrichment/02.5-01-SUMMARY.md`
</output>
