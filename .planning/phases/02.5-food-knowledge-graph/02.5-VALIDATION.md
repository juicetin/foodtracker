---
phase: 2.5
slug: food-knowledge-graph
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-14
---

# Phase 2.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (Python pipeline), Jest (TypeScript mobile) |
| **Config file** | `knowledge-graph/pytest.ini` (implicit), `apps/mobile/jest.config.js` |
| **Quick run command (Python)** | `cd knowledge-graph && python -m pytest tests/ -x` |
| **Quick run command (TS)** | `cd apps/mobile && npx jest --testPathPattern knowledge-graph -x` |
| **Full suite command** | `cd knowledge-graph && python -m pytest tests/ && cd ../../apps/mobile && npx jest` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd knowledge-graph && python -m pytest tests/ -x` (Python) or `cd apps/mobile && npx jest --testPathPattern knowledge-graph -x` (TS)
- **After every plan wave:** Run full suite command
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD | 01 | 1 | ML-04 | integration | `cd knowledge-graph && python -m pytest tests/test_kg_build.py -x` | W0 | pending |
| TBD | 01 | 1 | SC-1 | unit | `cd knowledge-graph && python -m pytest tests/test_kg_build.py::test_schema -x` | W0 | pending |
| TBD | 01 | 1 | ML-05 | integration | `cd knowledge-graph && python -m pytest tests/test_kg_build.py::test_worldcuisines_aliases -x` | W0 | pending |
| TBD | 02 | 1 | NUT-01 | unit | `cd apps/mobile && npx jest knowledgeGraphService.test -x` | W0 | pending |
| TBD | 02 | 1 | SC-4 | unit | `cd apps/mobile && npx jest symspellIndex.test -x` | W0 | pending |
| TBD | 03 | 2 | NUT-01 | integration | `cd apps/mobile && npx tsc --noEmit` | N/A | pending |
| TBD | 03 | 2 | SC-6 | checkpoint | Manual verification | N/A | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] `knowledge-graph/tests/test_kg_build.py` — stubs for ML-04, ML-05, SC-1 (schema, seeding, WorldCuisines)
- [ ] `apps/mobile/src/services/knowledge-graph/__tests__/knowledgeGraphService.test.ts` — stubs for NUT-01 (searchDish, getCanonicalRecipe, calculateDishNutrition)
- [ ] `apps/mobile/src/services/knowledge-graph/__tests__/symspellIndex.test.ts` — stubs for SC-4 (fuzzy matching)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| KG-powered nutrition shows in detection flow | NUT-01 | Requires full APK build + emulator + detection flow | Build APK, install on emulator, run detection, check logcat for KG tier output |
| KG database size under 70MB | Size budget | Requires full build pipeline completion | Run `build_kg.py`, check output file size |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
