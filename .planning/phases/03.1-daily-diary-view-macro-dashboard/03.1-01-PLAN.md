---
phase: 03.1-daily-diary-view-macro-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/services/diary/timePeriods.ts
  - apps/mobile/src/services/diary/diaryQueries.ts
  - apps/mobile/src/services/diary/__tests__/timePeriods.test.ts
  - apps/mobile/src/services/diary/__tests__/diaryQueries.test.ts
  - apps/mobile/src/store/usePreferencesStore.ts
  - apps/mobile/src/components/diary/StickyMacroHeader.tsx
  - apps/mobile/src/components/diary/WeekOverviewBar.tsx
  - apps/mobile/src/components/diary/TimePeriodSection.tsx
  - apps/mobile/src/components/diary/ExpandableEntryCard.tsx
  - apps/mobile/src/components/diary/index.ts
  - apps/mobile/src/components/diary/__tests__/ExpandableEntryCard.test.tsx
autonomous: true
requirements:
  - UI-01
  - UI-05

must_haves:
  truths:
    - "Time-period assignment correctly maps hours to morning/afternoon/evening with configurable boundaries"
    - "Entries for a date are loaded with timePeriod field assigned from created_at"
    - "Week overview data loads 7-day entry presence in a single SQL query"
    - "Preferences store persists diaryDisplayMode and timePeriodBoundaries across sessions"
    - "Entry card cycles through three states (summary -> ingredients -> collapsed) on tap"
    - "Macro header shows consumed values and toggles to remaining"
  artifacts:
    - path: "apps/mobile/src/services/diary/timePeriods.ts"
      provides: "Time-period assignment logic with configurable boundaries"
      exports: ["assignTimePeriod", "DEFAULT_BOUNDARIES", "TimePeriod", "TimePeriodBoundary"]
    - path: "apps/mobile/src/services/diary/diaryQueries.ts"
      provides: "Diary SQL queries: entries by date with timePeriod, week presence, day totals"
      exports: ["loadEntriesForDate", "loadWeekEntryPresence", "computeDayTotals"]
    - path: "apps/mobile/src/components/diary/ExpandableEntryCard.tsx"
      provides: "Three-state tap cycle entry card with photo thumbnail"
      exports: ["ExpandableEntryCard"]
    - path: "apps/mobile/src/components/diary/StickyMacroHeader.tsx"
      provides: "Pinned daily macro summary with consumed/remaining toggle"
      exports: ["StickyMacroHeader"]
    - path: "apps/mobile/src/components/diary/WeekOverviewBar.tsx"
      provides: "7-day bar showing entry presence per day"
      exports: ["WeekOverviewBar"]
  key_links:
    - from: "apps/mobile/src/services/diary/diaryQueries.ts"
      to: "apps/mobile/src/services/diary/timePeriods.ts"
      via: "assignTimePeriod import"
      pattern: "import.*assignTimePeriod.*from.*timePeriods"
    - from: "apps/mobile/src/components/diary/StickyMacroHeader.tsx"
      to: "apps/mobile/src/store/usePreferencesStore.ts"
      via: "reads diaryDisplayMode"
      pattern: "usePreferencesStore"
---

<objective>
Build the service layer (time-period logic, diary queries, preferences) and all new diary UI components (StickyMacroHeader, WeekOverviewBar, TimePeriodSection, ExpandableEntryCard) with unit tests. These are the building blocks that Plan 02 wires into the refactored DiaryScreen.

Purpose: Decouple logic from UI, establish testable contracts, create reusable components before screen-level integration.
Output: Tested service functions + 5 new components in apps/mobile/src/components/diary/
</objective>

<execution_context>
@/home/me/.claude/get-shit-done/workflows/execute-plan.md
@/home/me/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-daily-diary-view-macro-dashboard/03.1-CONTEXT.md
@.planning/phases/03.1-daily-diary-view-macro-dashboard/03.1-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From apps/mobile/src/types/index.ts:
```typescript
export interface FoodEntry {
  id: string;
  createdAt: string;
  entryDate: string;
  mealType: 'breakfast' | 'lunch' | 'dinner' | 'snack';
  photos: Photo[];
  ingredients: Ingredient[];
  totalCalories: number;
  totalProtein: number;
  totalCarbs: number;
  totalFat: number;
  notes?: string;
  updatedAt: string;
  isSynced: boolean;
  isDeleted: boolean;
}

export interface UserPreferences {
  region: 'AU' | 'US' | 'CA' | 'UK' | 'FR' | 'global';
  units: 'metric' | 'imperial';
  nutritionGoals: {
    calories: number;
    protein: number;
    carbs: number;
    fat: number;
  };
  darkMode: boolean;
}
```

From apps/mobile/src/store/usePreferencesStore.ts:
```typescript
interface PreferencesState extends UserPreferences {
  regionAutoDetected: boolean;
  setRegion: (region: UserPreferences['region']) => void;
  setUnits: (units: UserPreferences['units']) => void;
  setNutritionGoals: (goals: UserPreferences['nutritionGoals']) => void;
  setDarkMode: (darkMode: boolean) => void;
  initRegionFromLocale: () => void;
}
// Uses: create<PreferencesState>()(persist(..., { name: 'user-preferences', storage: createJSONStorage(() => AsyncStorage) }))
```

From apps/mobile/src/screens/DiaryScreen.tsx (existing data loading):
```typescript
// These patterns must be preserved/extracted:
function loadEntriesForDate(dateStr: string): DiaryEntry[]  // Queries food_entries + photos + scanned_dishes
function getTodayDateStr(): string  // new Date().toISOString().split('T')[0]
function dateToStr(d: Date): string  // d.toISOString().split('T')[0]
function formatDateLabel(dateStr: string): string  // 'Today'/'Yesterday'/formatted date
```

From apps/mobile/db/client.ts:
```typescript
export const opsqlite: OPSQLiteConnection;  // Raw SQL via opsqlite.executeSync()
```

Existing component patterns (StyleSheet conventions):
- Card: backgroundColor '#FFFFFF', borderRadius 12-16, padding 12-16, shadow
- Primary green: '#16A34A'
- Text primary: '#111827'
- Text secondary: '#6B7280'
- Text muted: '#9CA3AF'
- Macro colors: Protein '#3B82F6', Carbs '#D97706', Fat '#16A34A'
- Icons: Ionicons from @expo/vector-icons
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Service layer — time periods, diary queries, preferences store extension</name>
  <files>
    apps/mobile/src/services/diary/timePeriods.ts,
    apps/mobile/src/services/diary/diaryQueries.ts,
    apps/mobile/src/services/diary/__tests__/timePeriods.test.ts,
    apps/mobile/src/services/diary/__tests__/diaryQueries.test.ts,
    apps/mobile/src/store/usePreferencesStore.ts
  </files>
  <read_first>
    apps/mobile/src/screens/DiaryScreen.tsx,
    apps/mobile/src/store/usePreferencesStore.ts,
    apps/mobile/db/client.ts,
    apps/mobile/src/types/index.ts
  </read_first>
  <behavior>
    timePeriods.test.ts:
    - assignTimePeriod('2024-01-01T07:30:00') with defaults returns 'morning'
    - assignTimePeriod('2024-01-01T13:00:00') with defaults returns 'afternoon'
    - assignTimePeriod('2024-01-01T19:00:00') with defaults returns 'evening'
    - assignTimePeriod('2024-01-01T03:00:00') with defaults returns 'evening' (midnight-6am = evening per research)
    - assignTimePeriod('2024-01-01T10:00:00', { morning: 5, afternoon: 11, evening: 17 }) returns 'morning' (custom boundaries)
    - getTimePeriodLabel('morning') returns 'Morning'
    - getTimePeriodIcon('morning') returns 'sunny-outline'
    - getTimePeriodIcon('afternoon') returns 'partly-sunny-outline'
    - getTimePeriodIcon('evening') returns 'moon-outline'

    diaryQueries.test.ts (mock opsqlite):
    - loadEntriesForDate returns entries with timePeriod field assigned from created_at
    - loadWeekEntryPresence returns Map with date strings as keys
    - computeDayTotals sums calories/protein/carbs/fat from entry array
  </behavior>
  <action>
    1. Create `apps/mobile/src/services/diary/timePeriods.ts`:
       - Export `type TimePeriod = 'morning' | 'afternoon' | 'evening'`
       - Export `interface TimePeriodBoundary { morning: number; afternoon: number; evening: number; }`
       - Export `const DEFAULT_BOUNDARIES: TimePeriodBoundary = { morning: 6, afternoon: 12, evening: 18 }`
       - Export `const TIME_PERIOD_ORDER: TimePeriod[] = ['morning', 'afternoon', 'evening']`
       - Export `function assignTimePeriod(createdAt: string, boundaries = DEFAULT_BOUNDARIES): TimePeriod`
         Logic: extract hour from new Date(createdAt).getHours(). If hour >= boundaries.evening OR hour < boundaries.morning, return 'evening'. If hour >= boundaries.afternoon, return 'afternoon'. Else return 'morning'. (midnight-6am maps to evening)
       - Export `function getTimePeriodLabel(period: TimePeriod): string` — returns 'Morning'/'Afternoon'/'Evening'
       - Export `function getTimePeriodIcon(period: TimePeriod): string` — returns Ionicons names: 'sunny-outline'/'partly-sunny-outline'/'moon-outline'

    2. Create `apps/mobile/src/services/diary/diaryQueries.ts`:
       - Import `opsqlite` from `../../../db/client`
       - Import `assignTimePeriod, TimePeriod, TimePeriodBoundary, DEFAULT_BOUNDARIES` from `./timePeriods`
       - Export `interface DiaryDish { id: string; name: string; cuisine: string | null; }`
       - Export `interface DiaryEntry { id: string; timePeriod: TimePeriod; mealType: string; totalCalories: number; totalProtein: number; totalCarbs: number; totalFat: number; notes: string | null; createdAt: string; photoUri: string | null; dishes: DiaryDish[]; }`
       - Export `function loadEntriesForDate(dateStr: string, boundaries: TimePeriodBoundary = DEFAULT_BOUNDARIES): DiaryEntry[]`
         Same SQL as existing DiaryScreen.loadEntriesForDate but:
         - ORDER BY created_at ASC (not DESC — chronological within periods)
         - Assigns `timePeriod` via `assignTimePeriod(row.created_at, boundaries)`
       - Export `function loadWeekEntryPresence(centerDate: string): Map<string, number>`
         Single SQL: `SELECT entry_date, COUNT(*) as count FROM food_entries WHERE entry_date BETWEEN ? AND ? AND is_deleted = 0 GROUP BY entry_date`
         Calculate week boundaries: get the Monday of the week containing centerDate (Mon-Sun). Return Map<dateStr, count>.
       - Export `function computeDayTotals(entries: DiaryEntry[]): { calories: number; protein: number; carbs: number; fat: number }`
         Simple reduce summing totalCalories/totalProtein/totalCarbs/totalFat. Returns zeros for empty array.
       - Export `function getTodayDateStr(): string` — extracted from DiaryScreen
       - Export `function dateToStr(d: Date): string` — extracted from DiaryScreen
       - Export `function formatDateLabel(dateStr: string): string` — extracted from DiaryScreen

    3. Extend `apps/mobile/src/store/usePreferencesStore.ts`:
       - Import `TimePeriodBoundary, DEFAULT_BOUNDARIES` from `../services/diary/timePeriods`
       - Add to PreferencesState interface:
         `diaryDisplayMode: 'consumed' | 'remaining';`
         `timePeriodBoundaries: TimePeriodBoundary;`
         `setDiaryDisplayMode: (mode: 'consumed' | 'remaining') => void;`
         `setTimePeriodBoundaries: (b: TimePeriodBoundary) => void;`
       - Add defaults in create: `diaryDisplayMode: 'consumed'`, `timePeriodBoundaries: DEFAULT_BOUNDARIES`
       - Add actions: `setDiaryDisplayMode: (mode) => set({ diaryDisplayMode: mode })`, `setTimePeriodBoundaries: (b) => set({ timePeriodBoundaries: b })`

    4. Create test files with behaviors listed above. Mock `opsqlite` in diaryQueries tests using jest.mock('../../../db/client').
  </action>
  <verify>
    <automated>cd /home/me/repos/personal/foodtracker/apps/mobile && npx jest --testPathPattern="timePeriods|diaryQueries" --no-coverage -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function assignTimePeriod" apps/mobile/src/services/diary/timePeriods.ts` returns 1
    - `grep -c "export function loadEntriesForDate" apps/mobile/src/services/diary/diaryQueries.ts` returns 1
    - `grep -c "export function loadWeekEntryPresence" apps/mobile/src/services/diary/diaryQueries.ts` returns 1
    - `grep -c "export function computeDayTotals" apps/mobile/src/services/diary/diaryQueries.ts` returns 1
    - `grep "diaryDisplayMode" apps/mobile/src/store/usePreferencesStore.ts` matches at least 3 lines (type, default, action)
    - `grep "timePeriodBoundaries" apps/mobile/src/store/usePreferencesStore.ts` matches at least 3 lines
    - `grep "'evening'" apps/mobile/src/services/diary/timePeriods.ts` appears (midnight-6am maps to evening)
    - All tests in timePeriods.test.ts and diaryQueries.test.ts pass
  </acceptance_criteria>
  <done>
    Time-period assignment correctly handles all hours including midnight-6am (evening). Diary queries extract and extend existing DiaryScreen SQL with timePeriod field. Week presence loads in single query. Preferences store persists diaryDisplayMode and timePeriodBoundaries. All unit tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Diary UI components — StickyMacroHeader, WeekOverviewBar, TimePeriodSection, ExpandableEntryCard</name>
  <files>
    apps/mobile/src/components/diary/StickyMacroHeader.tsx,
    apps/mobile/src/components/diary/WeekOverviewBar.tsx,
    apps/mobile/src/components/diary/TimePeriodSection.tsx,
    apps/mobile/src/components/diary/ExpandableEntryCard.tsx,
    apps/mobile/src/components/diary/index.ts,
    apps/mobile/src/components/diary/__tests__/ExpandableEntryCard.test.tsx
  </files>
  <read_first>
    apps/mobile/src/services/diary/timePeriods.ts,
    apps/mobile/src/services/diary/diaryQueries.ts,
    apps/mobile/src/store/usePreferencesStore.ts,
    apps/mobile/src/screens/DiaryScreen.tsx,
    apps/mobile/src/types/index.ts
  </read_first>
  <action>
    1. Create `apps/mobile/src/components/diary/StickyMacroHeader.tsx`:
       - Props: `totals: { calories: number; protein: number; carbs: number; fat: number }`, `goals: { calories: number; protein: number; carbs: number; fat: number }`
       - Reads `diaryDisplayMode` and `setDiaryDisplayMode` from `usePreferencesStore`
       - Layout: white card with shadow, fixed above ScrollView (not inside it)
       - Top row: large calorie number (consumed or remaining based on mode) + "kcal" label + toggle button (Ionicons "swap-horizontal-outline")
       - When mode='consumed': show totals directly. When mode='remaining': show (goals - totals), clamp to 0 minimum
       - Bottom row: three MacroPill-style displays for P/C/F with same consumed/remaining logic
       - Colors: Protein '#3B82F6', Carbs '#D97706', Fat '#16A34A'
       - Toggle button onPress: calls setDiaryDisplayMode(current === 'consumed' ? 'remaining' : 'consumed')
       - Small label below toggle: "Consumed" or "Remaining" text indicating current mode

    2. Create `apps/mobile/src/components/diary/WeekOverviewBar.tsx`:
       - Props: `selectedDate: string`, `onSelectDate: (dateStr: string) => void`, `entryPresence: Map<string, number>`
       - Layout: horizontal row of 7 day circles (Mon-Sun)
       - Each circle shows: abbreviated day name (M/T/W/T/F/S/S), filled green (#16A34A) if entryPresence has that date, hollow gray (#E5E7EB border) if no entries
       - Selected date has a thicker border or ring (#16A34A border, 2px)
       - Today's date has a small dot below (even if not selected)
       - Calculate week dates: find Monday of the week containing selectedDate, generate 7 dates Mon-Sun
       - Tapping a day circle calls onSelectDate with that date string
       - Compact sizing: circles 36x36, gap 6

    3. Create `apps/mobile/src/components/diary/ExpandableEntryCard.tsx`:
       - Props: `entry: DiaryEntry` (from diaryQueries), `onNavigateToDetail: (entryId: string) => void`
       - Three-state tap cycle managed by local useState: 'summary' | 'ingredients' | 'collapsed'
       - Initial state: 'summary'
       - On tap: cycle summary -> ingredients -> collapsed -> summary
       - **State: 'collapsed'** — Show only: photo thumbnail (64x64, borderRadius 10) + dish names (1 line, ellipsized) + time. No macros.
       - **State: 'summary'** — Same as collapsed PLUS: calorie number + P/C/F row below dish names (compact, using existing MacroPill pattern)
       - **State: 'ingredients'** — Same as summary PLUS: expandable list of per-dish ingredients with individual name + amount_g + cal/P/C/F. Load ingredients from SQLite: `SELECT name, amount_g, calories, protein, carbs, fat FROM ingredients WHERE entry_id = ? ORDER BY calories DESC`
       - Photo handling: if entry.photoUri exists, show Image with onError fallback to Ionicons "restaurant-outline" placeholder (40x40, gray). If no photoUri, show placeholder directly.
       - Visual affordance for cycle state: three small dots at bottom-right of card. Dot matching current state is filled (#16A34A), others are hollow (#D1D5DB). This shows users where they are in the cycle.
       - Long-press: calls onNavigateToDetail(entry.id) — navigate to edit screen
       - Use react-native-reanimated `withTiming` for smooth height transitions between states (200ms, Easing.inOut(Easing.ease))

    4. Create `apps/mobile/src/components/diary/TimePeriodSection.tsx`:
       - Props: `period: TimePeriod`, `entries: DiaryEntry[]`, `onNavigateToDetail: (entryId: string) => void`
       - Layout: section header with icon (from getTimePeriodIcon) + label (from getTimePeriodLabel) + period calorie subtotal
       - Below header: list of ExpandableEntryCard components for each entry
       - If entries is empty, do not render anything (return null)
       - Period subtotals: sum totalCalories of entries in the period, show as "XXX kcal" in header right side
       - Also show P/F/C subtotals as small text under the calorie subtotal

    5. Create `apps/mobile/src/components/diary/index.ts`:
       Barrel export all components:
       ```
       export { StickyMacroHeader } from './StickyMacroHeader';
       export { WeekOverviewBar } from './WeekOverviewBar';
       export { TimePeriodSection } from './TimePeriodSection';
       export { ExpandableEntryCard } from './ExpandableEntryCard';
       ```

    6. Create `apps/mobile/src/components/diary/__tests__/ExpandableEntryCard.test.tsx`:
       - Test three-state cycle: render card, fireEvent.press 3 times, verify state transitions via testID or text visibility
       - Test: in 'collapsed' state, calorie number is NOT visible
       - Test: in 'summary' state, calorie number IS visible
       - Test: in 'ingredients' state, ingredient names appear (mock opsqlite to return ingredients)
       - Test: photo placeholder shown when photoUri is null (Ionicons "restaurant-outline")
       - Test: three dots indicator present with correct fills
  </action>
  <verify>
    <automated>cd /home/me/repos/personal/foodtracker/apps/mobile && npx jest --testPathPattern="ExpandableEntryCard" --no-coverage -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export.*StickyMacroHeader" apps/mobile/src/components/diary/StickyMacroHeader.tsx` returns 1
    - `grep -c "export.*WeekOverviewBar" apps/mobile/src/components/diary/WeekOverviewBar.tsx` returns 1
    - `grep -c "export.*TimePeriodSection" apps/mobile/src/components/diary/TimePeriodSection.tsx` returns 1
    - `grep -c "export.*ExpandableEntryCard" apps/mobile/src/components/diary/ExpandableEntryCard.tsx` returns 1
    - `grep "diaryDisplayMode" apps/mobile/src/components/diary/StickyMacroHeader.tsx` matches (reads preference)
    - `grep "swap-horizontal" apps/mobile/src/components/diary/StickyMacroHeader.tsx` matches (toggle icon)
    - `grep "'summary' | 'ingredients' | 'collapsed'" apps/mobile/src/components/diary/ExpandableEntryCard.tsx` or equivalent union type exists
    - `grep "restaurant-outline" apps/mobile/src/components/diary/ExpandableEntryCard.tsx` matches (photo placeholder)
    - `grep "getTimePeriodIcon" apps/mobile/src/components/diary/TimePeriodSection.tsx` matches (uses period icons)
    - Barrel index exports all 4 components
    - ExpandableEntryCard tests pass
  </acceptance_criteria>
  <done>
    All 5 diary components created with proper styling matching codebase conventions. StickyMacroHeader reads and toggles diaryDisplayMode from preferences store. WeekOverviewBar renders 7 day circles with entry presence indicators. ExpandableEntryCard implements three-state tap cycle with dot indicator affordance, photo thumbnails with fallback, and reanimated height transitions. TimePeriodSection groups entries with subtotals. All tests green.
  </done>
</task>

</tasks>

<verification>
cd /home/me/repos/personal/foodtracker/apps/mobile && npx jest --testPathPattern="timePeriods|diaryQueries|ExpandableEntryCard" --no-coverage
</verification>

<success_criteria>
- Time-period logic correctly maps all 24 hours to morning/afternoon/evening with configurable boundaries
- Diary queries are extracted from DiaryScreen and extended with timePeriod assignment
- Preferences store has diaryDisplayMode and timePeriodBoundaries persisted
- All 5 diary components (StickyMacroHeader, WeekOverviewBar, TimePeriodSection, ExpandableEntryCard, index.ts) exist and export correctly
- Three-state tap cycle on ExpandableEntryCard works with visual dot affordance
- Photo placeholder fallback handles missing photos
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-daily-diary-view-macro-dashboard/03.1-01-SUMMARY.md`
</output>
