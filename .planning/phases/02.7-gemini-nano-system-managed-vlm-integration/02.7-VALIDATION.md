---
phase: 2.7
slug: gemini-nano-system-managed-vlm-integration
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-18
---

# Phase 2.7 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest via `jest-expo` ~54.0.17 |
| **Config file** | `jest-expo` preset auto-configures (no explicit jest.config.js detected) |
| **Quick run command** | `cd apps/mobile && npx jest --testPathPattern="geminiNano" --no-coverage` |
| **Full suite command** | `cd apps/mobile && npx jest --no-coverage` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

| Milestone | Command |
|-----------|---------|
| Per task commit | `cd apps/mobile && npx jest --testPathPattern="geminiNano" --no-coverage` |
| Per wave merge | `cd apps/mobile && npx jest --no-coverage` |
| Phase gate | Full suite green + human checkpoint approved before Wave 2 |

---

## Wave 0 Gaps (must be created before test runs)

- [ ] `apps/mobile/src/services/vlm/__tests__/geminiNanoService.test.ts` — JSON parse, fallback, availability caching, raw output capture (5 tests)
- [ ] `apps/mobile/src/__mocks__/gemini-nano.ts` — mock for `geminiNanoModule` native module

---

## Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | Plan |
|--------|----------|-----------|-------------------|------|
| DET-03 | `checkAvailability()` returns `'available' \| 'downloading' \| 'not_supported'` | unit | `npx jest --testPathPattern="geminiNanoService"` | 02.7-01 |
| DET-03 | `identify()` parses valid JSON → `VlmFoodResult` | unit | `npx jest --testPathPattern="geminiNanoService"` | 02.7-01 |
| DET-03 | `identify()` handles JSON parse failure → `{ dishes: [] }` | unit | `npx jest --testPathPattern="geminiNanoService"` | 02.7-01 |
| DET-03 | Availability result is cached (no double native call per session) | unit | `npx jest --testPathPattern="geminiNanoService"` | 02.7-01 |
| DET-03 | GeminiNanoTestScreen renders without crashing | emulator | build + install + navigate via adb | 02.7-01 |
| DET-03 | Actual food identification quality ≥ 70% on 10-20 photos | device (human) | Human checkpoint on Pixel 9 Pro | 02.7-01 |
| DET-03 | `getLastRawOutput()` returns raw string after `identify()` | unit | `npx jest --testPathPattern="geminiNanoService"` | 02.7-01 |
| DET-03 | `runVlmIdentification()` uses Gemini Nano when available (Wave 2) | unit | `npx jest --testPathPattern="vlmPipeline"` | 02.7-02 |
| DET-03 | `runVlmIdentification()` falls through to SmolVLM when GeminiNano unavailable | unit | `npx jest --testPathPattern="vlmPipeline"` | 02.7-02 |
| DET-03 | `getLastVlmSource()` returns `'gemini-nano'` after Gemini Nano runs | unit | `npx jest --testPathPattern="vlmPipeline"` | 02.7-02 |
| DET-03 | DetectionScreen shows model source badge after detection | emulator | build + install + navigate + detect | 02.7-02 |
| DET-03 | "Raw Output" debug modal shows raw Gemini Nano JSON (device only) | device (human) | Manual on Pixel 9 Pro | 02.7-02 |

---

## Test Scope by Type

| Test Type | Scope | Notes |
|-----------|-------|-------|
| Unit (Jest) | TypeScript JSON parse logic, fallback behavior, `'available'` string comparison, availability caching logic | Mock `geminiNanoModule` native module — no native code needed |
| Emulator | Module loading, GeminiNanoTestScreen renders, navigation to GeminiNanoTest route | Inference will fail (emulator has no AICore) but UI render can be verified |
| Device only | Actual Gemini Nano inference, `FeatureStatus.AVAILABLE` response, JSON output quality | Manual on Pixel 9 Pro — this IS the human checkpoint |
