---
phase: 02.2-deploy-custom-335-class-classifier
plan: 02
subsystem: ml
tags: [tflite, inference-pipeline, efficientnet-lite0, imagenet-normalization, detection]

# Dependency graph
requires:
  - phase: 02.2-deploy-custom-335-class-classifier
    provides: "EfficientNet-Lite0 INT8 model, 335-class labels, CLASSIFY_CLASS_NAMES, IMAGENET_MEAN/STD constants, simplified ModelSet types"
provides:
  - "Two-stage inference pipeline (detect -> classify) replacing three-stage (binary -> detect -> classify + food101)"
  - "ImageNet normalization in imagePreprocess.ts for EfficientNet-Lite0 input"
  - "335-class food labels in detection output via formatClassLabel()"
  - "Confidence threshold (0.15) replacing binary gate for non-food filtering"
  - "DetectionScreen wired to 2-buffer preprocessing (640 detect + 224 classify)"
affects: [02.3, 02.4, 02.5, detection-screen, inference-pipeline]

# Tech tracking
tech-stack:
  added: []
  patterns: [two-stage-pipeline, imagenet-normalization-preprocess, confidence-threshold-fallback]

key-files:
  created: []
  modified:
    - apps/mobile/src/services/detection/inferenceRouter.ts
    - apps/mobile/src/services/detection/modelLoader.ts
    - apps/mobile/src/services/detection/imagePreprocess.ts
    - apps/mobile/src/screens/DetectionScreen.tsx
    - apps/mobile/src/services/detection/__tests__/inferenceRouter.test.ts
    - apps/mobile/src/services/detection/__tests__/modelBootstrap.test.ts
    - apps/mobile/src/services/detection/__tests__/modelLoader.test.ts

key-decisions:
  - "CLASSIFY_CONFIDENCE_THRESHOLD=0.15 for fallback label -- items always returned, just labeled 'Food Item' when low confidence"
  - "formatClassLabel generalizes formatFood101Label for any snake_case class name"
  - "classifyBuffer uses ImageNet normalization via preprocessImageForModel(..., 'imagenet')"

patterns-established:
  - "Two-stage pipeline: detect (YOLO 640x640) -> classify (EfficientNet-Lite0 224x224 ImageNet-normalized)"
  - "Normalization mode parameter on preprocessImageForModel: 'zero_one' (YOLO) or 'imagenet' (classifier)"

requirements-completed: [ML-01]

# Metrics
duration: 7min
completed: 2026-03-13
---

# Phase 02.2 Plan 02: Wire EfficientNet-Lite0 Classifier Summary

**Two-stage detection pipeline (detect -> classify) with 335-class EfficientNet-Lite0 labels and ImageNet normalization, replacing three-stage binary gate + food101 fallback**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-13T11:41:22Z
- **Completed:** 2026-03-13T11:48:38Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Replaced three-stage pipeline (binary gate -> detect -> classify + food101 fallback) with streamlined two-stage pipeline (detect -> classify)
- Added ImageNet normalization mode to imagePreprocess.ts for EfficientNet-Lite0 input (mean=[0.485,0.456,0.406], std=[0.229,0.224,0.225])
- Updated modelLoader to load exactly 2 models (detect + classify), removing binary gate and food101 model loading
- Eliminated all references to FOOD_V1_CLASS_NAMES, FOOD_101_CLASS_NAMES, BINARY_INPUT_SIZE, FOOD101_INPUT_SIZE from pipeline code
- All 84 detection tests pass across 7 test suites

## Task Commits

Each task was committed atomically:

1. **Task 1: Update pipeline logic (TDD RED)** - `2b13ef6c` (test)
2. **Task 1: Update pipeline logic (TDD GREEN)** - `c873ff81` (feat)
3. **Task 2: Wire DetectionScreen to new 2-stage pipeline** - `c06050d3` (feat)

## Files Created/Modified
- `apps/mobile/src/services/detection/inferenceRouter.ts` - Rewritten as 2-stage pipeline with 335-class output and confidence threshold
- `apps/mobile/src/services/detection/modelLoader.ts` - Simplified to 2-model loading (detect + classify)
- `apps/mobile/src/services/detection/imagePreprocess.ts` - Added 'imagenet' normalization mode parameter
- `apps/mobile/src/screens/DetectionScreen.tsx` - 2-buffer preprocessing with ImageNet normalization for classify
- `apps/mobile/src/services/detection/__tests__/inferenceRouter.test.ts` - Full rewrite for 2-stage pipeline tests
- `apps/mobile/src/services/detection/__tests__/modelBootstrap.test.ts` - Updated for 2-model bundled loading
- `apps/mobile/src/services/detection/__tests__/modelLoader.test.ts` - Updated for 2-model pack path loading

## Decisions Made
- CLASSIFY_CONFIDENCE_THRESHOLD set to 0.15 -- below this, items get "Food Item" fallback label rather than being rejected. This is intentionally NOT a binary gate: all COCO food detections are always returned
- formatClassLabel replaces formatFood101Label as a generalized snake_case-to-Title-Case formatter
- classifyBuffer preprocessing uses 'imagenet' normalization mode (new parameter on preprocessImageForModel)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated pre-existing modelLoader.test.ts for new 2-model API**
- **Found during:** Task 1
- **Issue:** modelLoader.test.ts (separate from modelBootstrap.test.ts) still tested old 3-model API (binary + detect + classify + food101)
- **Fix:** Rewrote test expectations for 2-model loading, removed mockBinaryModel and mockFood101Model
- **Files modified:** apps/mobile/src/services/detection/__tests__/modelLoader.test.ts
- **Verification:** All 84 detection tests pass
- **Committed in:** c873ff81 (Task 1 GREEN commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix)
**Impact on plan:** Necessary for correctness -- the old test file was testing a non-existent API shape. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 02.2 is fully complete: EfficientNet-Lite0 INT8 model deployed and wired into inference pipeline
- Detection pipeline runs as 2-stage (detect -> classify) with 335 food-specific class labels
- Ready for Phase 02.3 (food-specific YOLO detection) or Phase 02.4 (global cuisine training expansion)

---
*Phase: 02.2-deploy-custom-335-class-classifier*
*Completed: 2026-03-13*
